# 端到端流程文档

> **本文件职责**：描述从输入到输出的完整数据流和步骤逻辑  
> **函数接口详情**：见 `接口.md`  
> **目录结构**：见 `框架.md`

---

## 一、全局数据流

```
输入层
  lesson.md ──┐
  profile.yaml─┴──► [Step 1] Preprocessor.parse()
                              │ lesson_data: dict
                              │
         enable_experience?   │
              ├── Yes ──► [Step 0] ExperienceBank.retrieve()
              │                    │ experiences: list[ExperienceEntry]
              └── No  ──► experiences = []
                              │
                              ▼
             [Step 2] Roundtable.run_round0()          ← 4 Agent 并行
                              │ round0: dict[role_id, list[Annotation]]
                              ▼
             [Step 3] Roundtable.run_round1()          ← 真顺序 + 监控
                              │ round1: dict[role_id, list[Review]]
                              │ all_monitor_signals: list[MonitorSignal]
                              │
         enable_argument?     │
              ├── True  ──► [Step 3.5a] ArgumentGraphBuilder.build()
              │                    │ argument_graph: ArgumentGraph
              └── False ──► [Step 3.5b] ConflictClassifier.batch_classify()
                                   │ classified_conflicts: list[Conflict]
                              │
                              ▼
             [Step 4] ChairAgent.arbitrate()
                              │ verdict: Verdict
                              ▼
             [Step 5] Integrator.integrate()           ← 无 API，纯文本
                              │ draft_text: str
                              │ modifications: list[Modification]
                              │ (内部调用 _check_g1_full ← 1次API)
                              │
         enable_judge?        │
              ├── Yes ──► [Step 6] Judge.evaluate(draft_text)
              │                    │ score_report: ScoreReport
              │                    │
              │       score < threshold?
              │            ├── Yes ──► [Step 7] Integrator.refine(draft_text, score_report)
              │            │                    │ draft_text(更新), extra_mods
              │            └── No  ─────────────┤
              └── No  ─────────────────────────┤
                                                │
                              ▼                 │
             [Step 8] 写出文件 ◄────────────────┘
               write_polished()  write_process()
               validate_submission.run()  ← 失败→非零退出
                              │
         enable_experience?   │
              └── Yes ──► [Step 9] ExperienceBank.update()
```

---

## 二、各步骤说明

### Step 0 · 经验检索
- **条件**：`config["enable_experience"] == True`
- **消费**：`profile`、`lesson_data["text"]`
- **产出**：`experiences: list[ExperienceEntry]`（最多 top_k 条，按效用分排序）
- **调用**：`ExperienceBank.retrieve(profile, lesson_text, top_k=3)`，无 API

### Step 1 · 预处理
- **消费**：`lesson.md`、`profile.yaml`、CLI 的 `student_id`/`sample_id`
- **产出**：`lesson_data: dict`（结构见接口.md § Preprocessor）
- **副作用**：LaTeX 定界符为奇数 → 抛 `LaTeXError`，早失败
- **调用**：`Preprocessor(lesson_path, profile_path, student_id, sample_id).parse()`

### Step 2 · Round 0 并行批注
- **消费**：`lesson_data["text"]`、`lesson_data["profile"]`、`experiences`
- **产出**：`round0: dict[str, list[Annotation]]`，键为 role_id
- **并发**：4 个 Agent 通过 `ThreadPoolExecutor(max_workers=4)` 并行
- **API**：每角色 1 次，共 4 次（temperature：r_content=0.1，其余 0.3）
- **解析失败**：`_parse_json_with_schema()` 失败 → self-repair 重提示 → 3 次失败返回 `[]` + ERROR 日志

### Step 3 · Round 1 顺序互评 + 元认知监控
- **消费**：`round0`、`topo["round1_order"]`
- **产出**：`round1: dict[str, list[Review]]`、`all_monitor_signals: list[MonitorSignal]`
- **顺序**：后发言者接收 `prior_r1`（前序 Round 1 结果），实现真顺序信息传递
- **监控**（enable_monitor=true）：每轮发言后 `MetaCognitiveMonitor.check()` 检测幻觉/从众/同质化
  - `action_taken="suspend"` → `_handle_monitor_signals()` 要求 Agent 补证据（0-2 次 API）
- **API**：每角色 1 次顺序调用，共 4 次（temperature=0.3）

### Step 3.5a · 论辩图构建（enable_argument=true）
- **消费**：`round0`、`round1`、`lesson_data["text"]`
- **产出**：`argument_graph: ArgumentGraph`（含 arguments/attacks/supports/grounded_extension）
- **无 API**：`_build_arguments()` + `_build_relations()` + `_compute_grounded_extension()` 全为本地逻辑
- **排除条件**：quote 校验失败或 `rubric_anchor` 为空的批注不进图
- **降级**：所有论元被攻击或图为空 → 切换到 Step 3.5b

### Step 3.5b · 冲突分类（enable_argument=false 或降级）
- **消费**：`round0`、`round1`
- **产出**：`classified_conflicts: list[Conflict]`（含 FACTUAL/VALUE/INTERPRETATION 分类）
- **程序化兜底**：`_detect_by_quote_clash()` 检测同 location 不同 severity 的批注对
- **API**：每冲突 3 次采样（temperature=0.4），取多数

### Step 4 · 主持人仲裁
- **消费**：`argument_graph`（或 `classified_conflicts`）、`round0`、`round1`
- **产出**：`verdict: Verdict`（adopted/partial/rejected，每条含 `StructuredInstruction`）
- **约束**：Chair 只能在 `grounded_extension` 存活集内整合，无权推翻图计算结果
- **API**：1 次（temperature=0.2，max_tokens=8000）

### Step 5 · 确定性整合
- **消费**：`verdict.adopted + verdict.partial`、`lesson_data["text"]`（原文定位用）
- **产出**：`draft_text: str`、`modifications: list[Modification]`
- **无 API（主流程）**：`_locate_and_replace()` 精确/模糊匹配替换
  - 失败 → `_section_rewrite()` 章节级降级，记录 `quote_located=False`
  - 每次替换后 `_revalidate_remaining_quotes()` 检测 stale quote
- **副步骤（1次API）**：`_check_g1_full(draft_text)` 全文保真检测

### Step 6 · Judge 评审
- **消费**：`draft_text`（先经 `strip_adversarial_content()` 剥离元内容）
- **产出**：`score_report: ScoreReport`
- **API 明细**：
  - A/B/D/F：各 3 次采样（temperature=0.3）→ `_align_evidence()` 证据对齐 → `_score_from_evidence()`
  - C：3 次采样（temperature=0.2）→ 根因语义聚类 → `clamp(5 - Σ扣分, 0, 5)`
  - E：1 次（temperature=0.2）
  - D：额外 `_verify_d_quotes()` 归一化模糊匹配校验（无 API）
  - 可选：`StyleBiasAuditor.audit()` 6 次受控对（enable_bias_audit=true）

### Step 7 · 定向二次修订（条件触发）
- **触发**：`score_report["total"] < config["judge_threshold"]`
- **消费**：`draft_text`（★已磨课版，非原始）、`score_report["low_dims"]`
- **产出**：更新的 `draft_text`、`extra_modifications`
- **API**：每低分维度 1 次，最多 2 次（temperature=0.3）
- **维度→角色映射**：F→r_literacy，C→r_content，B→r_design，D→r_learner，A→r_design，E→规则后处理

### Step 8 · 写出文件
- **消费**：`draft_text`、`modifications`、`verdict`、`round0`、`round1`、`argument_graph`
- **产出**：`{prefix}_polished.md`、`{prefix}_process.json`
- **调用顺序**：
  1. `Integrator.write_polished(polished_path, draft_text)` — LaTeX 修复、外链图片去除
  2. `Integrator.write_process(process_path, mods, verdict, round0, round1, graph)` — 生成 process.json
  3. `validate_submission.run(out_dir)` — 契约四件套校验
  4. 校验 FAIL → `return 1`（非零退出）

### Step 9 · 经验库更新
- **条件**：`config["enable_experience"] == True` 且 `score_report` 有值
- **消费**：`process.json` 内容、`score_after`（Judge 总分）
- **产出**：更新 `experience_bank.json`（磁盘持久化）
- **无 API**：本地读写

---

## 三、API 调用汇总

| 步骤 | 函数 | 次数 | temperature |
|------|------|------|-------------|
| Step 2 | `BaseAgent.annotate()` | 4 并行 | 0.1 / 0.3 |
| Step 3 | `BaseAgent.peer_review()` | 4 顺序 | 0.3 |
| Step 3 | `_handle_monitor_signals()` | 0-2 | 0.3 |
| Step 3.5b | `ConflictClassifier.classify_one()` | ≤18 | 0.4 |
| Step 4 | `ChairAgent.arbitrate()` | 1 | 0.2 |
| Step 5 | `_check_g1_full()` | 1 | 0.0 |
| Step 6 | 各维度采样 | 16 | 0.2-0.3 |
| Step 6 | `StyleBiasAuditor._judge_once()` | 0/6 | 0.0 |
| Step 7 | `Integrator.refine()` | 0-2 | 0.3 |
| **合计** | | **约 50-58 次** | |

---

## 四、错误处理约定

| 异常 | 触发位置 | 处理 |
|------|---------|------|
| `LaTeXError` | Step 1 | 抛出→ run.py 捕获→非零退出 |
| JSON 解析失败 | Step 2/3 | self-repair→3次失败→`[]`+ERROR日志 |
| quote 定位失败 | Step 5 | 降级章节重写，`quote_located=False` |
| 论辩图为空 | Step 3.5a | 降级为 Step 3.5b |
| Monitor suspend | Step 3 | 要求补证据，最多 1 次修复 |
| G1 不通过 | Step 5 | WARNING+标记，不阻断 |
| `modifications` 为空 | Step 5 | `ValueError`→非零退出 |
| 阶段超时 | `run_phase()` | 跳过该阶段，保留已有结果 |
| 契约校验 FAIL | Step 8 | `return 1` |
