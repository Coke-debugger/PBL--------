# 团队分工方案（含创新点版）

> 版本：v2.0（已整合4个创新方向）  
> 团队规模：5人 · 参考周期：14天

---

## 一、整体逻辑

本方案将创新研究任务与工程实现任务绑定分配——每人既有"跑通系统"的工程责任，也有"做出新意"的研究责任。两类任务共用代码，不额外增加工作量。

| 创新方向              | 负责人       | 与工程任务的绑定                      |
| ----------------- | --------- | ----------------------------- |
| 方向1：专家角色蒸馏+知识边界测试 | **P2**    | 提示词工程本身就要迭代，测OOS顺手            |
| 方向2：冲突类型分类+差异化消解  | **P4**    | 整合器要处理verdict，直接在其中实现分类器      |
| 方向3：多智能体量规优化偏差    | **P3**    | Judge实现后立即测量量规优化偏差ROB，组织超量规盲评 |
| 方向4：学情自适应开放度控制    | **P2+P5** | P2加提示词，P5设计实验验证可控性            |

---

## 二、五人分工总览

| 编号     | 角色       | 核心职责                | 创新责任                           | 主要交付物                                             |
| ------ | -------- | ------------------- | ------------------------------ | ------------------------------------------------- |
| **P1** | 架构师      | 系统骨架、调度器、CLI        | 无（保障其他4人能并行）                   | `run.py` `roundtable.py` `base_agent.py`          |
| **P2** | 提示词工程师   | 5个角色提示词设计与校准        | **方向1**（OOS边界）+ **方向4**（开放度控制） | `prompts/` `agents/*.py` `openness_calculator.py` |
| **P3** | Judge工程师 | LLM-as-Judge评审组件    | **方向3**（量规优化偏差ROB测量）           | `judge.py` `rob_measurer.py` 盲评数据表                |
| **P4** | 整合器工程师   | 修改整合、process.json生成 | **方向2**（冲突类型分类+差异化消解）          | `integrator.py` `conflict_classifier.py`          |
| **P5** | 实验&报告    | 基线、实验、技术报告、答辩       | 4个方向的实验数据汇总与分析                 | `baseline.py` 实验数据 技术报告 PPT                       |

---

## 三、接口契约（Day 1定好，全员并行的前提）

> **P1在第1天下班前发布** `interfaces.md`，所有人遵守，不允许后期改字段名。

### Agent批注输出格式（P2遵守）

```json
[{
  "issue_id": "F-01",
  "dimension": "F",
  "severity": "major",
  "location": "项目目标·第1条",
  "quote": "了解蜡烛燃烧原理",
  "problem": "目标为知识点罗列，无行为动词",
  "suggestion": "改写为：能从露营情境中提出问题并拆解影响变量",
  "in_scope": true
}]
```

> `in_scope`字段新增：P2的OOS边界测试用，False表示Agent主动标记超出职责。

### 冲突分类输出格式（P4生成，P1的roundtable.py消费）

```json
{
  "conflict_type": "FACTUAL|VALUE|INTERPRETATION",
  "confidence": 0.85,
  "view_a": {"role_id": "r_content", "issue_id": "C-01", "stance": "方程式错误"},
  "view_b": {"role_id": "r_literacy", "issue_id": "F-02", "stance": "无需改"},
  "resolution_method": "three_sample_verify|weight_vote|rubric_anchor"
}
```

### Judge评分输出格式（P3生成，P5消费）

```json
{
  "total": 72.5,
  "dimension_scores": {"A":4.0,"B":3.2,"C":4.5,"D":3.8,"E":4.1,"F":3.3},
  "low_dims": ["F", "B"],
  "ROB": 3.2,
  "delta_rubric": 12.5,
  "delta_extra": 9.3
}
```

> `ROB`/`delta_rubric`/`delta_extra`为新增字段，支持方向3量规优化偏差实验。

---

## 四、各成员详细职责

### P1 · 架构师 / 技术负责人

**适合特质**：Python熟练，擅长模块化设计，能快速搭出可用骨架。

**工程职责**：

- [ ] Day 1：定义并发布接口契约
- [ ] Day 1-2：`base_agent.py`（API调用、重试、JSON解析）
- [ ] Day 2-3：`preprocessor.py`（Markdown结构解析、YAML解析）
- [ ] Day 3-4：`roundtable.py`（调度3轮研讨，含并行Round 0）
- [ ] Day 4-5：`run.py`（CLI接口，连接所有模块）
- [ ] Day 6-7：联调集成，确保全流程跑通

**创新支持（为P4提供插槽）**：在`roundtable.py`的Round 1→Round 2之间，预留冲突分类调用点：

```python
# roundtable.py Round 1结束后
conflicts = self.detect_conflicts(round0, round1)       # P1提供骨架
classified = conflict_classifier.batch_classify(conflicts)  # P4实现
verdict = self.chair.arbitrate(round0, round1, classified)  # P1传入
```

**关键代码（并行Round 0）**：

```python
import concurrent.futures

def run_round0(self) -> dict:
    with concurrent.futures.ThreadPoolExecutor(max_workers=4) as ex:
        futures = {
            rid: ex.submit(agent.annotate,
                          self.lesson_data["text"],
                          self.lesson_data["profile"])
            for rid, agent in self.agents.items()
        }
        return {rid: f.result() for rid, f in futures.items()}
```

---

### P2 · 提示词工程师 + 方向1&4研究员

**适合特质**：细心、愿意反复迭代、深读了量规和金标准案例。

> ⭐ 磨课效果的核心，也是最有研究深度的岗位之一。

#### 工程职责

- [ ] Day 1-2：精读附录A量规（重点F维度）和附录B五个案例
- [ ] Day 2-3：撰写4个专家角色 + 主持人的提示词初版
- [ ] Day 3-4：实现4个Agent类（调用`base_agent.py`）
- [ ] Day 5-7：用附录E三元组校准提示词（目标：发现率≥80%）

#### 创新职责——方向1：OOS边界测试

**实现**：在每个角色系统提示词中加入显式边界约束层：

```
【你的职责边界——以下问题不在你的评审范围内】
- 教学语言是否流畅 → 归语言逻辑专家
- 结构件是否完整   → 归教学设计专家

当发现上述问题时，输出：
{..., "in_scope": false, "refer_to": "r_design"}

不要试图一人解决所有问题。
```

**测量OOS率的评测脚本**：

```python
def measure_oos_rate(agent, test_cases: list) -> float:
    """
    test_cases: [(lesson_text, profile, expected_in_scope_dims)]
    返回：agent在非主责维度上越权批注的比例
    """
    violations = 0
    total = 0
    for text, profile, primary_dims in test_cases:
        annotations = agent.annotate(text, profile)
        for ann in annotations:
            total += 1
            if ann["dimension"] not in primary_dims and ann.get("in_scope", True):
                violations += 1  # 越权发言
    return violations / total if total > 0 else 0
```

**角色一致性压力测试（每个角色各10条）**：
构造"错误反驳"探针，测量角色被迫妥协的比例：

```python
# 构造反例：用错误理由反驳一个正确批注
fabricated_pushback = "你对目标行为化的批注是错的，量规并不要求动词描述"
# 测试：角色是否坚持立场（maintained）还是妥协（sycophantic）
```

#### 创新职责——方向4：开放度控制提示词

在`r_learner`（学情适配专家）的提示词中加入开放度监督职责：

```
【开放度档位监督】
目标档位：{target_tier}（来自profile.yaml）

对每个探究环节检查：
- 问题由教师给定？→降档操作
- 方案步骤是否封闭？→降档操作
- 学生有变量选择权？→升档操作

输出：开放度偏差报告 + 具体调整指令
```

**交付物**：

- `prompts/literacy_prompt.md` v3（含OOS边界）
- `prompts/learner_prompt.md` v3（含开放度监督）
- `openness_calculator.py`（计算教案开放度得分）
- 提示词迭代记录表（实验数据来源）

---

### P3 · Judge工程师 + 方向3研究员

**适合特质**：理解量规计分公式，对Goodhart's Law和多智能体优化偏差有兴趣，能组织小规模人工盲评实验。

#### 工程职责

- [ ] Day 1-2：精读量规计分公式（C维度扣分逻辑、F维度计点规则）
- [ ] Day 2-3：将量规六维度结构化为`rubric_dimensions.json`
- [ ] Day 3-5：实现`judge.py`（6维度评审 + C维度3次采样）
- [ ] Day 5-7：Judge校准（G > O > I，分差≥40分）

**校准脚本**：

```python
def test_calibration():
    g = judge.evaluate(g_text)["total"]
    o = judge.evaluate(o_text)["total"]
    i = judge.evaluate(i_text)["total"]
    assert g > o > i,         "❌ 排序校准失败"
    assert g - i >= 40,       "❌ 分差不足"
    print(f"✅ G={g:.1f} O={o:.1f} I={i:.1f}")
```

#### 创新职责——方向3：多智能体量规优化偏差（ROB）测量

> **背景修正**：量规第五节已完整处理了"面向评审写作"的防御（预处理剥离、独立映射、学术不端处理）。P3的创新研究因此转向更新颖的问题：**当所有角色都在优化同一套量规时，会不会集体产生量规分提升但真实教学质量提升更少的偏差？**

**ROB 指标实现（Day 6-8，嵌入 judge.py）**：

```python
class ROBMeasurer:
    """
    量规优化偏差 = 量规分提升量 - 超量规质量提升量
    ROB > 0 → 偏差存在（刷了量规分但真实质量提升不足）
    ROB ≈ 0 → 量规是良好代理
    """
    def measure(self, baseline_score: float, multiagent_score: float,
                baseline_extra: float, multiagent_extra: float) -> dict:
        delta_rubric = multiagent_score - baseline_score
        delta_extra  = multiagent_extra - baseline_extra
        rob = delta_rubric - delta_extra
        return {
            "delta_rubric": delta_rubric,
            "delta_extra":  delta_extra,
            "ROB": rob,
            "interpretation": (
                "偏差显著（刷分）" if rob > 5
                else "量规是良好代理" if abs(rob) <= 5
                else "量规低估了真实提升"
            )
        }
```

**超量规质量评估（Day 7-8，人工盲评）**：

- 请3位同学/老师盲读 baseline 和多智能体输出，不告知哪份是哪个系统产出
- 评估4个量规未覆盖维度：课堂可执行性 / 情境创意 / 认知梯度 / 学科思维保真性
- 各维度5级评分，取平均作为 extra_quality 分

**ROB实验（Day 9-10）**：

```
输入：附录E化学案例 I 版（运行5次取均值）
条件A：baseline.py → 量规分 Q_r0，超量规分 Q_e0
条件B：多智能体系统 → 量规分 Q_r1，超量规分 Q_e1
ROB = (Q_r1 - Q_r0) - (Q_e1 - Q_e0)

同时分析：每条批注是否被量规关键词框架主导（rubric_anchored_ratio）
```

**交付物**：

- `judge.py`（含 ROBMeasurer + 基础Judge评分）
- ROB实验数据表（量规分/超量规分/偏差值）
- 人工盲评原始评分表（供技术报告引用）

---

### P4 · 整合器工程师 + 方向2研究员

**适合特质**：细心，熟悉JSON处理，能理解冲突的本质差异。

#### 工程职责

- [ ] Day 1：精读附录D契约细则
- [ ] Day 2-3：实现`integrator.py`骨架（定位+修改教案原文）
- [ ] Day 3-4：实现`process.json`生成逻辑
- [ ] Day 4-5：G1保真检测 + G2改劣检测
- [ ] Day 5-6：用`validate_submission.py`验证格式合规

**契约红线**：

```
⚠️ modifications 数组不能为空
⚠️ 禁止删除带错内容而不替换（G2：修复义务）
⚠️ 两文件必须成对，学号/样本ID无下划线
```

#### 创新职责——方向2：冲突类型分类器

**实现`conflict_classifier.py`**：

```python
class ConflictClassifier:
    PROMPT = """分析两位教研专家的不同意见，判断冲突类型：

Agent A：{view_a}
Agent B：{view_b}

冲突类型（三选一）：
- FACTUAL：有唯一正确答案（可查课标/教材验证）
- VALUE：合理目标之间的取舍，无唯一答案
- INTERPRETATION：同一条量规的不同解读

输出JSON：{"conflict_type": "...", "confidence": 0.0-1.0, "reasoning": "..."}"""

    def classify(self, view_a, view_b, contention) -> dict:
        results = [self._single(view_a, view_b) for _ in range(3)]  # 3次采样
        return self._majority(results)


class TypeAwareResolver:
    """依据冲突类型选择消解策略"""

    WEIGHTS = {"F":30,"C":20,"B":15,"D":15,"A":10,"E":10}

    def resolve(self, conflict, conflict_type, rubric) -> dict:
        if conflict_type == "FACTUAL":
            return self._three_sample_verify(conflict)   # 三次采样验证
        elif conflict_type == "VALUE":
            return self._weight_vote(conflict)            # 量规权重表决
        else:  # INTERPRETATION
            return self._rubric_anchor(conflict, rubric)  # 量规原文锚定

    def _weight_vote(self, conflict) -> dict:
        dim_a = conflict["view_a"]["dimension"]
        dim_b = conflict["view_b"]["dimension"]
        winner = dim_a if self.WEIGHTS[dim_a] >= self.WEIGHTS[dim_b] else dim_b
        return {
            "decision": f"采纳{winner}维度立场",
            "rationale": f"量规权重 {winner}({self.WEIGHTS[winner]}分) >= "
                         f"{dim_b}({self.WEIGHTS[dim_b]}分)"
        }
```

**消解效果实验（Day 7-8）**：

- 从附录B五案例人工构造30条冲突（每类10条）
- 专家标注"正确解法"为ground truth
- 对比：随机选 / 多数投票 / 统一LLM仲裁 / 类型匹配消解
- 指标：Resolution Accuracy（与专家标注的一致率）

**交付物**：

- `conflict_classifier.py`（分类器+差异化消解器）
- `integrator.py`（含类型感知仲裁）
- 冲突消解实验数据表

---

### P5 · 实验&报告负责人

**适合特质**：写作能力强，善于设计实验、呈现数据，能做PPT。

#### 工程职责

- [ ] Day 1-3：撰写技术报告框架（不等系统完成）
- [ ] Day 2-4：实现`baseline.py`（单模型对照）
- [ ] Day 5-6：设计并运行基础对照实验（基线 vs 多智能体）

#### 研究职责——4个方向的实验汇总

**实验1（方向2）：冲突消解准确率**

```
数据集：P4构造的30条冲突案例（已有专家标注）
条件：随机/多数投票/统一仲裁/类型匹配消解
指标：Resolution Accuracy
```

**实验2（方向3）：量规优化偏差ROB**

```
数据集：同一 I 版教案，baseline 和多智能体各跑5次
条件：量规分（Judge自动）vs 超量规质量（P3组织3人盲评）
指标：ROB = ΔQ_rubric - ΔQ_extra
```

**实验3（方向1）：角色OOS越权率**

```
数据集：P2标注的测试案例集（主责维度 vs 越权批注）
条件：无边界约束 vs 三层锚点注入
指标：OOS_rate，Recall@primary_dim
```

**实验4（方向4）：开放度可控性**

```
输入：固定I版教案，target_tier分别设1/2/3/4
条件：4档开放度 × 重复3次
指标：|计算开放度 - 目标开放度| < 0.15
同时验证：Judge质量分在4档间波动 < 10分
```

**实验5：基线 vs 多智能体总体效果**

```
条件：baseline.py vs 完整系统
输入：附录E化学案例 + 自建数学/生物案例（共3个）
指标：Judge总分 + 六维度分项对比
```

**技术报告结构**：

```
1. 任务概述（任务定义、输入/输出、难点）
2. 系统设计
   2.1 总体架构
   2.2 量规→角色设计的转化逻辑（方向1方法论基础）
3. 创新点
   3.1 冲突类型分类与差异化消解（方向2）
   3.2 多智能体量规优化偏差（方向3）
   3.3 角色知识边界与OOS测试（方向1）
   3.4 学情自适应开放度控制（方向4，可作future work）
4. 实验（含上述5组实验数据及分析）
5. 局限性与可拓展方向
6. 参考文献
```

**交付物**：

- `baseline.py`
- 5组实验数据（Excel或Markdown表格）
- `report/技术报告.md`（含答辩15分钟分工：汇报3分+方法论3分+创新实验3分+演示3分+问答3分）
- 答辩PPT（含现场演示脚本）

> ⚠️ **端到端格式验证责任（P1+P4，Day 11-12 必须完成）**  
> 
> ```bash
> # P4 负责单份验证
> python validate_submission.py outputs_practice/
> # → 必须全部 PASS（无 FAIL，WARN 可接受）
> 
> # P1 负责全流程模拟验收方批量运行
> python run.py --lesson 附录/附录E_公开练习三元组/I_降质版_化学_应急蜡烛.md \
>              --profile profile_chem01.yaml \
>              --out outputs_practice/ \
>              --student-id {学号} --sample-id CHEM01
> python validate_submission.py outputs_practice/
> ```
> 
> 两个验证都通过后，outputs_practice/ 才可提交。

---

## 五、时间线（含创新任务）

```
         P1-架构师        P2-提示词+方向1&4  P3-Judge+方向3   P4-整合器+方向2   P5-实验报告
Day 1    接口契约          读量规+案例         读量规公式         读附录D          报告框架
Day 2    base_agent       写提示词初版        rubric.json       integrator骨架   baseline.py
Day 3    preprocessor     实现Agent类         judge.py          process.json     实验设计
Day 4    roundtable骨架   OOS边界加入提示词    C维度采样         冲突分类器骨架    实验1准备
Day 5    run.py           校准提示词①         校准Judge①        G1/G2检测        实验2准备
Day 6    联调集成          OOS测试集构建       攻击变体构建①      类型感知消解      运行baseline
Day 7    ↕联调            校准提示词②+开放度  攻击变体构建②      消解实验(方向2)   实验1运行
Day 8    ↕debug           开放度Prompt测试    ROB盲评组织        format验证        实验2运行
Day 9    性能优化          方向1实验数据汇总   防御效果分析        practice样本      实验3/4运行
Day 10   代码整理          提示词定版          Judge文档          process.json示例  数据整理
Day 11   README           ←←←←←←←←←←← 联合集成测试 →→→→→→→→→→→→→→→→→→→→→→
Day 12   答辩演示准备      ←←←←←←←←←←← 全流程端到端验证 →→→→→→→→→→→→→→→→→→→
Day 13   代码提交整理                                                              PPT制作
Day 14   答辩                                                                       答辩
```

---

## 六、依赖关系图（含创新模块）

```
P1 [基础框架 + roundtable调度插槽]
     │
     ├──► P2 [角色提示词 + OOS边界 + 开放度控制]
     │         │
     │         └── 方向1实验数据 ──────────────────► P5 [汇总实验]
     │
     ├──► P3 [Judge + 三层防御 + 攻击套件]
     │         │
     │         └── ROB实验数据 ────────────────────► P5 [汇总实验]
     │
     ├──► P4 [整合器 + 冲突分类器 + 类型消解]
     │         │
     │         └── 消解准确率数据 ─────────────────► P5 [汇总实验]
     │
     └──► P5 [基线 + 实验设计 + 报告 + PPT]
               │
               └── 开放度可控性实验 ◄─── P2 开放度Prompt
```

**唯一强阻塞**：P2/P3/P4/P5 都需要 P1 的 `base_agent.py` 接口（Day 1接口契约解除大部分）。其余均可并行。

---

## 七、答辩分工（15分钟）

| 环节              | 负责人   | 时长  | 内容要点                              |
| --------------- | ----- | --- | --------------------------------- |
| 任务概述+架构         | P1    | 2分钟 | 端到端流程图、模块拓扑                       |
| 方法论转化+角色设计（方向1） | P2    | 3分钟 | 量规→角色的转化逻辑、OOS越权率实验结果             |
| 创新实验（方向2+3）     | P4+P3 | 3分钟 | 冲突分类准确率、ASR攻击实验数据                 |
| 现场演示            | P1+P2 | 3分钟 | `python run.py` 跑一次完整磨课           |
| 问答              | 全员    | 4分钟 | 按方向分配：方向1→P2，方向2→P4，方向3→P3，方向4→P5 |

---

## 八、风险预案

| 风险           | 预案                                  |
| ------------ | ----------------------------------- |
| P2提示词校准超时    | 先保证F/C两高权重维度，其余降级；OOS实验数据不足时用定性分析代替 |
| P3攻击实验时间不够   | 至少完成A2（关键词堆砌）和A5（提示词注入）两种最典型攻击      |
| P4冲突分类器准确率低  | 低准确率本身是发现（说明机器冲突分类是未解问题），写进失败分析可获高分 |
| 方向4开放度实验时间不够 | 降级为定性分析+future work，不影响其他3个方向的完整实验  |
| API额度紧张      | P3攻击实验用轻量Judge（减少采样次数），P5实验优先运行基线对比 |

---

*分工方案 v2.0 · 2026-07-16 · 已整合4个创新方向*
