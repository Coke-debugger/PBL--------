# 接口规范文档

> **本文件职责**：所有类和函数的完整签名、参数表、返回值  
> **流程说明**：见 `流程.md` | **目录结构**：见 `框架.md`  
> 格式约定：`✅必需` `❌可选`，返回值用 `->` 标注，负责人用 **P1-P5** 标注

---

## 一、分工责任表

| 文件 | 负责人 |
|------|--------|
| `code/run.py` | **P1** |
| `code/core/types.py` `core/trace.py` `core/cache.py` | **P1** |
| `code/core/preprocessor.py` `core/roundtable.py` `core/agents/registry.py` `core/agents/base_agent.py` | **P1** |
| `code/core/agents/literacy/content/learner/design/chair_agent.py` | **P2** |
| `code/core/judge.py` `modules/bias_auditor.py` `modules/rob_measurer.py` `modules/redteam.py` | **P3** |
| `code/core/integrator.py` `core/validate_submission.py` `modules/argument_graph.py` | **P4** |
| `code/modules/monitor.py` `modules/experience_bank.py` `baseline.py` `experiments/exp*.py` | **P5** |

---

## 二、数据类型（`core/types.py`，P1负责）

| 类型 | 关键字段 |
|------|---------|
| `PipelineConfig` | `enable_round0/round1/argument/judge/monitor/experience` `active_roles` `judge_threshold` `timeouts:dict` |
| `TopologyConfig` | `round1_order:list` `round1_visibility:"sequential"\|"parallel"` `convergence_eps:float` |
| `RunTrace` | `call_id` `step` `model` `prompt_hash` `latency_ms` `input_tokens` `output_tokens` `parse_ok:bool` `retries:int` |
| `Profile` | `subject` `grade` `prior_knowledge` `learning_motivation` `target_openness_tier:int` |
| `Annotation` | `issue_id` `dimension:"A"\|"B"\|"C"\|"D"\|"E"\|"F"` `severity:"major"\|"minor"` `location` `quote` `problem` `suggestion` `in_scope:bool` `refer_to:str\|None` `rubric_anchor:str\|None` |
| `Review` | `refers_to:str(issue_id)` `stance:"支持"\|"质疑"\|"补充"\|"分歧"` `content` `round:int` `role_id` `in_scope:bool` `refer_to:str\|None` |
| `Argument` | `arg_id` `issue_id` `claim` `evidence_quote` `rubric_anchor` `dimension` `role_id` |
| `ArgumentGraph` | `arguments:list[Argument]` `attacks:list[AttackRelation]` `supports:list[SupportRelation]` `grounded_extension:list[str]` |
| `MonitorSignal` | `signal_type:"hallucination"\|"sycophancy"\|"homogeneity"` `role_id` `issue_id:str\|None` `evidence` `action_taken:"suspend"\|"warn"\|"pass"` |
| `ExperienceEntry` | `exp_id` `condition` `content` `utility:float(0-1)` `source` |
| `StructuredInstruction` | `target_quote:str(≤60字)` `replacement_text` `section_fallback` |
| `Decision` | `issue_id` `decision:"adopted"\|"partial"\|"rejected"` `rationale` `instruction:StructuredInstruction\|None` `arg_id:str\|None` |
| `Verdict` | `adopted:list[Decision]` `partial:list[Decision]` `rejected:list[Decision]` `surviving_args:list[str]` |
| `Modification` | `mod_id` `location` `before_summary` `after_summary` `source_role` `rationale` `quote_located:bool` |
| `DimEvidence` | `sub_indicator` `status:"满足"\|"部分满足"\|"不满足"` `evidence_quote` `reasoning` `quote_verified:bool` |
| `ScoreReport` | `total:float` `dimension_scores:dict[str,float]` `low_dims:list[str]` `evidence:dict` `judge_version:str` `bias_audit:dict\|None` `ROB:float\|None` |

---

## 三、`run.py`（P1）

### `parse_args() -> argparse.Namespace`
| 参数 | 类型 | 必需 | 说明 |
|------|------|------|------|
| `--lesson` | str | ✅ | 教案 .md 路径 |
| `--profile` | str | ✅ | 学情 .yaml 路径 |
| `--out` | str | ✅ | 输出目录 |
| `--student-id` | str | ❌ 默认STU001 | 学号 |
| `--sample-id` | str | ❌ 默认SAMPLE01 | 样本ID |
| `--config` | str | ❌ 默认configs/pipeline.yaml | 流水线配置 |
| `--topology` | str | ❌ 默认configs/topology.yaml | 拓扑配置 |
| `--no-judge` | flag | ❌ | 跳过 Judge |

### `load_config(path: str) -> dict`
- 读取 YAML，返回 `PipelineConfig` 或 `TopologyConfig` 字典

### `run_phase(phase_name: str, fn: Callable, *args, **kwargs) -> Any | None`
| 参数 | 类型 | 说明 |
|------|------|------|
| `phase_name` | str | 对应 `config["timeouts"]` 的 key |
| `fn` | Callable | 待执行函数 |
- **返回**：fn 的结果；超时返回 `None` + WARNING 日志（不抛异常）

### `main() -> int`
- **返回**：`0`成功，`1`任意失败
- **调用顺序**（见 `流程.md`）：`parse_args → load_config×2 → Preprocessor.parse → ExperienceBank.retrieve(可选) → Roundtable.run → Integrator.integrate → Judge.evaluate(可选) → Integrator.refine(可选) → write_polished → write_process → validate_submission.run → ExperienceBank.update(可选)`

---

## 四、`core/preprocessor.py`（P1）

### `Preprocessor.__init__(lesson_path, profile_path, student_id, sample_id)`
| 参数 | 类型 | 说明 |
|------|------|------|
| `lesson_path` | str | 教案文件路径 |
| `profile_path` | str | 学情 YAML 路径 |
| `student_id` | str | 用于 meta.student_id |
| `sample_id` | str | 用于 meta.sample_id |

### `Preprocessor.parse() -> dict`
- **返回**：`lesson_data` 完整格式：
```python
{
  "text": str,          # 教案原文
  "profile": Profile,
  "structure": {
    "sections": list[dict],   # [{"level":int,"heading":str,"content":str}]
    "course_type": str,       # "PBL" | "常规课"
    "subject": str,
    "grade_level": str,
  },
  "meta": {"student_id": str, "sample_id": str},
  "self_detected_errors": list[dict],  # [{"location","quote","error_type","root_cause"}]
}
```
- **抛出**：`LaTeXError` 若 `$` 定界符为奇数个

### `Preprocessor._extract_sections(text: str) -> list[dict]`
- **返回**：`[{"level":int, "heading":str, "content":str}]`

### `Preprocessor._detect_obvious_errors(text: str, profile: dict) -> list[dict]`
- **返回**：`[{"location","quote","error_type":"major"|"minor","root_cause"}]`（穷人版G2用）

---

## 五、`core/agents/base_agent.py`（P1）

### `BaseAgent.__init__(role_id: str, name: str, expertise: str)`

### `BaseAgent.get_system_prompt() -> str`（抽象，由子类实现）

### `BaseAgent.call(user_message: str, temperature: float = 0.3) -> str`
| 参数 | 类型 | 说明 |
|------|------|------|
| `user_message` | str | user role 的消息内容 |
| `temperature` | float | ❌ 默认0.3 |
- **返回**：模型响应文本
- **重试**：最多3次，指数退避
- **缓存**：temperature=0.0 时查/写 `ResponseCache`

### `BaseAgent.annotate(lesson_text: str, profile: dict, experiences: list = []) -> list[Annotation]`
| 参数 | 说明 |
|------|------|
| `lesson_text` | 教案全文 |
| `profile` | 学情字典 |
| `experiences` | 经验库检索结果（可空） |
- **返回**：`list[Annotation]`（JSON 解析失败返回 `[]` + ERROR 日志）

### `BaseAgent.peer_review(lesson_text: str, own_r0: list, others_r0: dict, prior_r1: dict = {}) -> list[Review]`
| 参数 | 说明 |
|------|------|
| `own_r0` | 自己的 Round 0 批注 |
| `others_r0` | 其他角色的 Round 0 批注（dict[role_id, list]）|
| `prior_r1` | 前序发言者的 Round 1（真顺序用）|
- **返回**：`list[Review]`

### `_parse_json_with_schema(raw: str, schema_type) -> list | None`
| 参数 | 说明 |
|------|------|
| `raw` | 模型原始输出文本 |
| `schema_type` | TypedDict 类型（用于校验）|
- **返回**：解析成功返回列表，失败返回 `None`

---

## 六、`core/agents/`专家角色（P2）

每个专家角色继承 `BaseAgent`，实现以下方法：

### `XxxAgent.get_system_prompt() -> str`
- **返回**：角色系统提示词（含 OOS 边界约束、量规判据、正/负例锚点）
- **读取**：`prompts/xxx_prompt.md`（通过 `manifest.json` 计算版本哈希）

### `XxxAgent._build_annotation_prompt(lesson_text, profile, experiences) -> str`
- **返回**：完整 user message 字符串（含教案全文、学情、经验）

### `XxxAgent._build_review_prompt(own_r0, others_r0, prior_r1) -> str`
- **返回**：互评 user message 字符串

**各角色主责维度**：

| 角色 | role_id | 主责维度 | temperature |
|------|---------|---------|-------------|
| LiteracyAgent | `r_literacy` | F（30分）| 0.3 |
| ContentAgent | `r_content` | C（20分）| 0.1 |
| LearnerAgent | `r_learner` | D（15分）| 0.3 |
| DesignAgent | `r_design` | A+B（25分）| 0.3 |
| ChairAgent | `r_chair` | 整合+仲裁 | 0.2 |

### `ChairAgent.arbitrate(all_annotations, all_reviews, argument_graph) -> Verdict`
| 参数 | 类型 | 说明 |
|------|------|------|
| `all_annotations` | `dict[str, list[Annotation]]` | round0 |
| `all_reviews` | `dict[str, list[Review]]` | round1 |
| `argument_graph` | `ArgumentGraph` | 论辩图（含存活集）|
- **返回**：`Verdict`（每条 Decision 含 `StructuredInstruction`）
- **约束**：Chair 只能在 `grounded_extension` 存活集内整合

---

## 七、`core/roundtable.py`（P1）

### `Roundtable.__init__(lesson_data: dict, config: PipelineConfig, topo: TopologyConfig)`

### `Roundtable.run(experiences: list = []) -> tuple[dict, dict, ArgumentGraph, Verdict]`
- **返回**：`(round0, round1, argument_graph, verdict)`
- **内部依次调用**：`run_round0 → run_round1 → ArgumentGraphBuilder.build（或降级）→ ChairAgent.arbitrate`

### `Roundtable.run_round0(config, experiences) -> dict[str, list[Annotation]]`
- **并发**：`ThreadPoolExecutor(max_workers=len(active_roles))`
- **超时**：受 `config["timeouts"]["round0"]` 控制

### `Roundtable.run_round1(round0, config, topo) -> tuple[dict, list[MonitorSignal]]`
- **返回**：`(round1, all_monitor_signals)`
- **顺序**：按 `topo["round1_order"]` 顺序，传入 `prior_r1`

### `Roundtable._safe_annotate(role_id, agent, experiences) -> list[Annotation]`
- JSON 解析失败时 self-repair 重提示，3次失败返回 `[]` + ERROR 日志

### `Roundtable._safe_review(role_id, agent, own_r0, others_r0, prior_r1) -> list[Review]`
- 同上，3次失败返回 `[]`

### `Roundtable._handle_monitor_signals(role_id, agent, reviews, signals) -> list[Review]`
- `action_taken="suspend"` 时要求 Agent 补证据，最多1次修复

---

## 八、`core/integrator.py`（P4）

### `Integrator.__init__(lesson_data, verdict, student_id, sample_id)`

### `Integrator.integrate() -> tuple[str, list[Modification]]`
- **返回**：`(draft_text, modifications)`
- 对每条 adopted/partial Decision 调用 `_locate_and_replace` 或降级 `_section_rewrite`
- 内部调用 `_check_g1_full`（1次API）和 `_check_g2_self`（无API）

### `Integrator.refine(draft_text: str, score_report: ScoreReport) -> tuple[str, list[Modification]]`
| 参数 | 类型 | 说明 |
|------|------|------|
| `draft_text` | str | ★已磨课版本，非原始教案 |
| `score_report` | ScoreReport | Judge评分报告 |
- **返回**：`(updated_draft_text, extra_modifications)`
- 对 `score_report["low_dims"]` 中每个维度调用对应角色补写

### `Integrator.write_polished(path: Path, text: str) -> None`
- 写出前：`_fix_latex_pairs(text)` + `_remove_external_images(text)`
- 编码：UTF-8 无 BOM

### `Integrator.write_process(path, mods, verdict, round0, round1, argument_graph) -> None`
| 参数 | 类型 | 说明 |
|------|------|------|
| `path` | Path | 输出路径 |
| `mods` | list[Modification] | 修改记录（minItems≥1，否则报错）|
| `verdict` | Verdict | 仲裁结果 |
| `round0` | dict | Round 0 批注（discussion 数据源）|
| `round1` | dict | Round 1 互评（discussion 数据源）|
| `argument_graph` | ArgumentGraph | 论辩图 |
- **输出**：`process.json`（完整格式见 `附录D`）

### `Integrator._locate_and_replace(text: str, quote: str, replacement: str) -> tuple[bool, str]`
- 先精确匹配，再 `_normalize()` 后模糊匹配
- **返回**：`(定位成功:bool, 替换后文本:str)`

### `Integrator._normalize(text: str) -> str`
- 全角转半角，多余空格合并，统一换行

### `Integrator._section_rewrite(text: str, section: str, content: str) -> str`
- 找到 `section` 对应的标题，替换整个节内容（降级策略）

### `Integrator._revalidate_remaining_quotes(text: str, remaining: list[Decision]) -> None`
- 检测剩余 Decision 的 `target_quote` 是否仍可在当前 text 中定位
- 失败时标记 `instruction["stale"] = True`，后续 integrate 跳过并 WARNING

### `Integrator._check_g1_full(output_text: str) -> dict`
- **返回**：`{"pass": bool, "issues": list[dict]}`
- 调用 Claude API 1次（temperature=0.0），全文分块比对

### `Integrator._check_g2_self() -> list[dict]`
- 对 `lesson_data["self_detected_errors"]` 中每条错误检查是否已被修复
- **返回**：`[{"error": dict, "type": "unhandled", "penalty": str}]`

### `Integrator._build_discussion(round0, round1) -> list[dict]`
- 将 round0/round1 的发言整理为 process.json 的 `discussion` 数组格式

### `Integrator._find_source_role(issue_id: str) -> str`
- 在 round0 中反查 issue_id 对应的 role_id

---

## 九、`core/judge.py`（P3）

### `Judge.__init__(profile: Profile, config: PipelineConfig)`

### `Judge.evaluate(lesson_text: str) -> ScoreReport`
| 参数 | 类型 | 说明 |
|------|------|------|
| `lesson_text` | str | 教案文本（内部先调 `strip_adversarial_content()`）|
- **返回**：`ScoreReport`
- **API 调用**：16次（A/B/D/F各3次 + C×3 + E×1）

### `Judge._sample_evidence(dim: str, text: str, n: int = 3) -> list[list[DimEvidence]]`
| 参数 | 说明 |
|------|------|
| `dim` | 维度字母 "A"\|"B"\|"D"\|"F" |
| `n` | 采样次数 |
- **返回**：`n` 组证据列表（每组一个完整维度的子指标结果）
- **temperature**：0.3

### `Judge._align_evidence(evidence_sets: list[list[DimEvidence]]) -> list[DimEvidence]`
| 参数 | 说明 |
|------|------|
| `evidence_sets` | n 次采样的证据集合 |
- **返回**：对齐去重后的单一证据列表（多数投票决定 `status`）

### `Judge._evaluate_c_dimension(text: str, n: int = 3) -> dict`
- **返回**：`{"score": float, "evidence": list[dict], "confirmed_issues": list[str]}`
- 3次采样 → 根因语义聚类 → ≥2/3 确认才扣分
- **temperature**：0.2

### `Judge._evaluate_dimension_once(dim: str, text: str) -> dict`
- 仅 E 维度使用（单次采样）
- **返回**：`{"score": float, "evidence": list[DimEvidence]}`

### `Judge._score_from_evidence(dim: str, evidence: list[DimEvidence]) -> float`
- 按量规公式计算：`5 × Σ点数 / 满点数`（C 维度用 `clamp(5-Σ扣分,0,5)`）
- **返回**：`float`（值域 [0,5]）

### `Judge._verify_d_quotes(evidence: list[DimEvidence], lesson_text: str) -> list[DimEvidence]`
- 对 D 维度每条证据做归一化模糊匹配校验
- 校验失败 → `quote_verified=False` + `status` 降为"部分满足"
- **返回**：更新后的证据列表

### `Judge._compute_prompt_hash() -> str`
- 读 `prompts/manifest.json`，对所有文件 sha256 拼接后哈希
- **返回**：12位十六进制字符串（作为 `ScoreReport.judge_version`）

### `strip_adversarial_content(text: str) -> tuple[str, list[dict]]`（模块级函数）
- 移除自述性元内容（"设计意图"节、自带映射表、自评分）
- 检测提示词注入，标记疑似内容
- **返回**：`(净化后文本, 被移除内容列表)`

---

## 十、`modules/argument_graph.py`（P4）

### `ArgumentGraphBuilder.build(round0, round1, lesson_text) -> ArgumentGraph`
- **返回**：含 grounded_extension 的完整论辩图
- **无 API**：纯本地逻辑

### `ArgumentGraphBuilder._build_arguments(round0) -> list[Argument]`
- 过滤条件：quote 校验失败 或 `rubric_anchor` 为空 → 排除

### `ArgumentGraphBuilder._build_relations(arguments, round1) -> tuple[list[AttackRelation], list[SupportRelation]]`
- 从 `Review.stance` 提取：stance="分歧" → AttackRelation；stance="支持" → SupportRelation

### `ArgumentGraphBuilder._compute_grounded_extension(arguments, attacks) -> list[str]`
- Dung grounded semantics，迭代最小不动点算法
- **返回**：存活论元的 `arg_id` 列表

---

## 十一、`modules/monitor.py`（P5）

### `MetaCognitiveMonitor.check(role_id, reviews, round0, prior_r1) -> list[MonitorSignal]`
| 参数 | 说明 |
|------|------|
| `role_id` | 当前发言角色 |
| `reviews` | 该角色本轮互评 |
| `round0` | 所有角色的 Round 0 批注 |
| `prior_r1` | 前序发言者的 Round 1（dict）|
- **返回**：`list[MonitorSignal]`（3项检测：幻觉/从众/同质化）
- **无 API**（信号检测为本地逻辑）

---

## 十二、`modules/experience_bank.py`（P5）

### `ExperienceBank.retrieve(profile, lesson_text, top_k=3) -> list[ExperienceEntry]`
| 参数 | 说明 |
|------|------|
| `profile` | Profile 学情字典 |
| `lesson_text` | 教案全文（用于课型检测和关键词匹配）|
| `top_k` | 返回的最大条目数 |
- **返回**：按 `utility × keyword_sim` 降序排列的经验列表
- **无 API**

### `ExperienceBank.update(sample_id, process_json, score_before, score_after) -> None`
| 参数 | 说明 |
|------|------|
| `sample_id` | 样本ID（用于 exp_id 命名）|
| `process_json` | 本次 process.json 内容（dict）|
| `score_before` | 磨课前 Judge 分（可传 0 若无基线）|
| `score_after` | 磨课后 Judge 分 |
- **无 API**；持久化到 `experience_bank.json`

---

## 十三、`modules/bias_auditor.py`（P3）

### `StyleBiasAuditor.audit(lesson_text, n_pairs=6) -> BiasAuditResult`
- 生成 `n_pairs` 组受控对（格式变体/篇幅变体），分别调用轻量 Judge 评分
- **返回**：`BiasAuditResult`（`verdict="biased"` 若偏差 > 5 分）
- **API**：`n_pairs × 2` 次（temperature=0.0）

---

## 十四、`core/validate_submission.py`（P4）

### `run(out_dir: str) -> dict`
| 参数 | 说明 |
|------|------|
| `out_dir` | 待校验目录路径 |
- **返回**：`{"has_fail": bool, "failures": list[str], "warnings": list[str]}`

### 内部校验函数

| 函数 | 参数 | 返回 | 说明 |
|------|------|------|------|
| `check_naming(filename)` | str | "PASS"\|"FAIL" | 学号/样本ID 规则 |
| `check_encoding(filepath)` | str | "PASS"\|"FAIL" | UTF-8 + 非空 |
| `check_latex(text)` | str | "PASS"\|"FAIL" | `$` 定界符奇偶 |
| `check_images(text)` | str | "PASS"\|"WARN" | 外链图片检测 |
| `check_schema(json_path)` | str | "PASS"\|"FAIL" | process.json Schema 校验 |
| `check_pairs(dir_path)` | str | "PASS"\|"FAIL" | .md/.json 成对出现 |

---

## 十五、`core/agents/registry.py`（P1）

### `build_agents(active_roles: list[str]) -> dict[str, BaseAgent]`
| 参数 | 说明 |
|------|------|
| `active_roles` | 来自 `PipelineConfig.active_roles` |
- **返回**：`{"r_literacy": LiteracyAgent(), ...}`（仅实例化 active 的角色）
- 未知 role_id 自动忽略（不报错）

```python
AGENT_REGISTRY = {
    "r_literacy": LiteracyAgent,
    "r_content":  ContentAgent,
    "r_learner":  LearnerAgent,
    "r_design":   DesignAgent,
    "r_chair":    ChairAgent,
}
```

---

## 十六、跨文件调用依赖图

```
run.py
  ├── core/preprocessor.py          (P1)
  ├── modules/experience_bank.py    (P5)  [可选]
  ├── core/roundtable.py            (P1)
  │     ├── core/agents/*.py        (P2)
  │     ├── modules/monitor.py      (P5)  [可选]
  │     └── modules/argument_graph.py (P4) [可选]
  ├── core/integrator.py            (P4)
  │     └── core/validate_submission.py (P4)
  ├── core/judge.py                 (P3)
  │     └── modules/bias_auditor.py (P3)  [可选]
  └── modules/experience_bank.py    (P5)  [可选]

experiments/*.py
  ├── modules/argument_graph.py     (P4)
  ├── modules/redteam.py            (P3)
  ├── modules/rob_measurer.py       (P3)
  └── core/judge.py                 (P3)
```

> **核心规则**：`core/` 内部模块不 import `modules/`，只通过 `PipelineConfig` 开关动态 import。`experiments/` 可直接 import 任意模块，但不被主流水线依赖。
