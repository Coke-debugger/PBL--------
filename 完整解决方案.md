# 多智能体教案磨课系统——完整解决方案

> 课题：大模型 Agent 应用开发实践——基于多智能体圆桌研讨的教案智能磨课系统  
> 版本：v1.0 · 2026-07  
> 技术栈：Python 3.11 · Claude API (claude-opus-4-8) · 多智能体框架（自研）

---

## 目录

1. [系统概述与设计原则](#一系统概述与设计原则)
2. [技术架构](#二技术架构)
3. [专家角色设计](#三专家角色设计——量规驱动的角色建模)
4. [研讨工作流](#四研讨工作流)
5. [核心代码实现](#五核心代码实现)
6. [LLM-as-Judge 自动评审组件](#六llm-as-judge-自动评审组件)
7. [创新点详述](#七创新点详述)
8. [对照实验设计](#八单模型-vs-多智能体对照实验)
9. [局限性与可拓展方向](#九局限性与可拓展方向)
10. [AI 协作开发记录（CLAUDE.md）](#十ai-协作开发记录)
11. [参考文献](#十一参考文献)

---

## 一、系统概述与设计原则

### 1.1 问题界定

当前 AI 教案改写工具普遍采用"单模型一键改写"范式——单次提示词调用，缺乏真实教研圆桌中多专业视角的碰撞与互校。其主要缺陷为：

| 缺陷类型   | 具体表现                                         |
| ------ | -------------------------------------------- |
| 维度盲区   | 单一提示词难以同时兼顾内容准确性（C维）、素养导向性（F维）、学情适配（D维）等六个维度 |
| 无自校机制  | 生成错误无法被内部发现与纠正                               |
| 修改不可追溯 | 改了什么、为什么改，无法解释                               |
| 素养贴标签  | 目标行为化不足，"素养词"与活动脱节                           |

本系统通过**多专家角色分工 + 跨轮互评 + LLM-as-Judge 反馈闭环**，系统性解决上述缺陷。

### 1.2 设计原则

1. **量规驱动角色建模**：角色职责直接对应评价量规六个维度，每个角色是对应维度的最优判断者。
2. **最小信息共享**：各角色第一轮独立批注，避免锚定效应；第二轮才看他人发言。
3. **修改可追溯性**：每条修改绑定来源角色、来源轮次、引用原文三元组，写入 `process.json`。
4. **防退化保真检测**：磨课前后做内容保真检查（G1）与改劣检测（G2），禁止"删掉错误"策略。

---

## 二、技术架构

### 2.1 系统流程图

```
输入层
  ├── lesson.md          (待磨课教案，Markdown)
  └── profile.yaml       (学情描述)
         │
         ▼
  ┌─────────────────────────────────────────────┐
  │            预处理层 (preprocessor.py)         │
  │  解析教案结构 / 提取学科学段 / 检测已有错误     │
  └─────────────────────────────────────────────┘
         │
         ▼
  ┌─────────────────────────────────────────────────────────────────┐
  │                  多智能体圆桌研讨层 (roundtable.py)               │
  │                                                                  │
  │  Round 0  独立批注（并行，无信息共享）                             │
  │    ├── Agent A · 素养导向教研员  → annotation_A.json             │
  │    ├── Agent B · 学科内容专家    → annotation_B.json             │
  │    ├── Agent C · 学情适配专家    → annotation_C.json             │
  │    └── Agent D · 教学设计专家    → annotation_D.json             │
  │                                                                  │
  │  Round 1  跨角色互评（顺序发言，看到所有Round 0结果）              │
  │    ├── Agent A 互评 B/C/D 的批注 → reply_A_r1.json              │
  │    ├── Agent B 互评 A/C/D 的批注 → reply_B_r1.json              │
  │    ├── Agent C 互评 A/B/D 的批注 → reply_C_r1.json              │
  │    └── Agent D 互评 A/B/C 的批注 → reply_D_r1.json              │
  │                                                                  │
  │  Round 2  分歧仲裁（主持人Agent汇总冲突，输出裁定）                │
  │    └── Agent E · 主持人/仲裁者   → verdict.json                 │
  │                                                                  │
  └─────────────────────────────────────────────────────────────────┘
         │
         ▼
  ┌─────────────────────────────────────────────┐
  │         整合层 (integrator.py)               │
  │  依据裁定结果逐条修改教案 / 生成process.json   │
  └─────────────────────────────────────────────┘
         │
         ▼
  ┌─────────────────────────────────────────────┐
  │      LLM-as-Judge 反馈闭环 (judge.py)        │
  │  对输出教案按量规六维评分 / 触发二次修订       │
  └─────────────────────────────────────────────┘
         │
         ▼
输出层
  ├── {学号}_{样本ID}_polished.md    (磨课后教案)
  └── {学号}_{样本ID}_process.json  (研讨过程记录)
```

### 2.3 模块间数据结构规范（接口契约补全）

> `preprocessor.parse()` 返回 `lesson_data` 字典，所有模块依赖此结构，格式如下：

```python
lesson_data = {
    "text":    str,          # 教案原始Markdown全文（UTF-8）
    "profile": {             # 学情描述（来自profile.yaml）
        "subject":               str,   # 学科，如"化学"
        "grade":                 str,   # 年级，如"初中"
        "prior_knowledge":       str,   # 先验知识描述
        "learning_motivation":   str,   # 学习动机，如"中等"
        "target_openness_tier":  int,   # 目标开放度档位1-4 ★方向4
    },
    "structure": {           # 解析后的教案结构件
        "sections": list,    # [(heading, content), ...]
        "course_type": str,  # "PBL" | "常规课"
        "subject": str,
        "grade_level": str,
    },
    "meta": {                # 验收用元数据
        "student_id": str,   # 学号（仅含字母数字）
        "sample_id":  str,   # 样本ID（字母数字连字符）
    }
}
```

```
{学号}_final/
├── code/
│   ├── run.py                # CLI入口（统一接口）
│   ├── preprocessor.py       # 输入解析与结构提取
│   ├── roundtable.py         # 多智能体圆桌调度器
│   ├── agents/
│   │   ├── base_agent.py     # 基础Agent类
│   │   ├── literacy_agent.py # 素养导向教研员
│   │   ├── content_agent.py  # 学科内容专家
│   │   ├── learner_agent.py  # 学情适配专家
│   │   ├── design_agent.py   # 教学设计专家
│   │   └── chair_agent.py    # 主持人/仲裁者
│   ├── judge.py              # LLM-as-Judge评审组件（含三层防御）★方向3
│   ├── integrator.py         # 修改整合与输出生成
│   ├── conflict_classifier.py # 冲突类型分类+差异化消解器 ★方向2
│   ├── rob_measurer.py       # 量规优化偏差测量器          ★方向3
│   ├── openness_calculator.py # 开放度计算与控制          ★方向4
│   ├── baseline.py           # 单模型基线（对照实验用）
│   ├── schemas/
│   │   └── process_schema.json  # process.json Schema
│   ├── prompts/              # 提示词模板
│   │   ├── literacy_prompt.md   # 含OOS边界约束层         ★方向1
│   │   ├── content_prompt.md    # 含OOS边界约束层         ★方向1
│   │   ├── learner_prompt.md    # 含开放度监督职责         ★方向4
│   │   ├── design_prompt.md     # 含OOS边界约束层         ★方向1
│   │   ├── chair_prompt.md      # 含类型感知仲裁指令       ★方向2
│   │   └── judge_prompt.md      # Judge评审提示词
│   ├── resources/
│   │   └── rubric_dimensions.json  # 量规维度结构化数据
│   ├── experiments/          # 实验脚本
│   │   ├── exp1_conflict_resolution.py  # 方向2实验
│   │   ├── exp2_asr_attack.py           # 方向3实验
│   │   ├── exp3_oos_rate.py             # 方向1实验
│   │   └── exp4_openness_control.py     # 方向4实验
│   ├── requirements.txt
│   └── README.md
├── outputs_practice/
│   ├── {学号}_CHEM01_polished.md
│   └── {学号}_CHEM01_process.json
├── report/
│   └── {学号}_技术报告.md
├── ai_collab/
│   ├── CLAUDE.md
│   ├── prompts/
│   └── notes.md
└── logs/
```

---

## 三、专家角色设计——量规驱动的角色建模

### 3.1 角色-维度映射表

> 设计原则：每个角色的核心职责唯一对应量规的最高权重维度，且专业背景差异化，避免同质化堆砌。

| 角色ID         | 角色名     | 主责维度                                 | 专业背景设定                             | 话术风格                                               |
| ------------ | ------- | ------------------------------------ | ---------------------------------- | -------------------------------------------------- |
| `r_literacy` | 素养导向教研员 | **F · 素养导向性 (30分)**                  | 熟悉各学科课程标准素养体系，擅长目标行为化与思维显性化设计      | 引用课标原文，质疑"贴标签"式素养目标，追问"学生被要求做了什么"                  |
| `r_content`  | 学科内容专家  | **C · 内容准确性 (20分)**                  | 学科博士背景，对知识点、公式、实验方案有严苛的准确性标准       | 精确引用原文错误位置，给出正确写法，拒绝含糊                             |
| `r_learner`  | 学情适配专家  | **D · 内容一致性 (15分)** + **开放度控制 ★方向4** | 认知科学与教育心理学背景，关注目标-活动-评价链条一致性与开放度适配 | 核对目标与活动的认知层次匹配，**同时监督开放度是否符合学情档位**                 |
| `r_design`   | 教学设计专家  | **A+B · 结构完整性+内容丰富性 (25分)**          | 资深教学设计师，熟悉PBL项目课与常规课结构规范           | 检查结构件完备性，追问预设回答的具体性，发现"不可执行"的教学过程                  |
| `r_chair`    | 主持人/仲裁者 | **整合 + 类型感知仲裁 ★方向2**                 | 教研组长，擅长多方观点综合与冲突裁定                 | 中立客观，**依据冲突类型（FACTUAL/VALUE/INTERPRETATION）差异化裁定** |

> ★ **方向1（OOS边界）**：所有角色系统提示词均含"职责边界"约束层，显式列出越权问题类型及转交指令。批注JSON新增 `in_scope` 字段，`false` 时自动转交对应专家。

### 3.2 角色提示词设计（以素养导向教研员为例）

```
你是一位资深的【素养导向教研员】，专注于中小学课程标准核心素养体系。
你的职责是对教案进行批注，重点关注以下问题：

【你的核心判断标准——量规F维度操作化】
1. 目标行为化（F1/F2）
   - 每条教学目标是否包含"能/会/掌握"等行为动词 + 具体认知行为描述？
   - 素养词（科学思维/数学抽象等）是否绑定了对应的学科活动，而非贴标签？
   - 参照该学科该学段课标素养体系核查（见下方提供的素养参照表）

2. 思维显性化（F3）
   - 教案中是否有环节要求学生表达"怎么想到的/为什么这样设计"？
   - 该表达是否有教师预设应对，还是孤立插入一句"说说你怎么想的"？

3. 情境真实性（F4）
   - 导入情境是否在后续≥2个环节实质使用（任务围绕情境/数据来自情境）？
   - 还是仅作装饰性导入，后续活动与情境脱节？

4. 整合性主活动（F4加强，PBL课型）
   - 是否存在要求学生综合≥2个知识点/方法并自主组织解决路径的主任务？
   - 还是每步都由教师给出封闭式小问题？

5. 评价可落地性（F6）
   - 量规的水平描述是否能由本课活动实际检验？
   - PBL课型：是否有个人成长成果与问题解决成果的双轨量规？

【输出格式要求】
以JSON数组输出，每条批注格式：
{
  "issue_id": "F-01",
  "dimension": "F",
  "severity": "major|minor",
  "location": "教案中的章节/句子",
  "quote": "原文引用（≤30字）",
  "problem": "问题描述",
  "suggestion": "具体修改建议（不得仅说'加强素养导向'）"
}
```

### 3.3 案例方法论→角色话术的转化逻辑

从附录B五个金标准案例提炼的共性方法论特征，转化为各角色的判断依据：

| 案例特征                     | 转化为角色判据                  | 归属角色       |
| ------------------------ | ------------------------ | ---------- |
| 化学案例：4条目标含"能从XXX中抽象出YYY" | F1判据：目标须含"能+行为+情境内容"三元素  | r_literacy |
| 数学案例：描点法→建模流程完整          | B维判据：探究环节须有完整"问题-数据-结论"链 | r_design   |
| 生物案例：调查→分析→设计→实施→迭代      | F4判据：PBL任务链须是问题解决的自然展开   | r_literacy |
| 化学案例：预设含"预期答案要点+典型误区应对"  | B维判据：预设回答须含具体内容，非"学生答：是" | r_design   |
| 所有案例：量规水平描述针对本项目         | F6判据：量规须含可检验的项目专属指标      | r_literacy |

---

## 四、研讨工作流

### 4.1 完整研讨时序

```
Step 1  预处理
  - 解析教案Markdown结构（提取标题树、各节正文）
  - 解析学情YAML（学段、学科、学情特征）
  - 检测LaTeX定界符配对（契约预检）
  - 生成"教案结构摘要"供各Agent参考

Step 2  Round 0：独立批注（并行，互不可见）
  并行调用 r_literacy / r_content / r_learner / r_design
  每个Agent：输入=[教案全文 + 学情 + 自身系统提示 + 素养参照表]
  输出：结构化批注JSON（含issue_id/severity/quote/suggestion）

Step 3  Round 1：跨角色互评（顺序发言）
  顺序：r_literacy → r_content → r_learner → r_design
  每个Agent：输入=[自己的Round 0批注 + 其他三人的Round 0批注]
  任务：
    a) 支持/质疑/补充他人批注（须给出具体理由）
    b) 标记"分歧项"（自己认为重要但他人未提，或与他人意见相左）
    c) OOS标记：若发现他人批注超出其主责维度，标注 "out_of_scope"  ★方向1
  输出：互评JSON，含 refers_to + in_scope 字段

Step 3.5  冲突类型分类 ★方向2（新增步骤）
  conflict_classifier.py 自动识别 Round 0→Round 1 中的分歧对：
    - 提取所有"stance不一致"的批注对
    - 对每对运行3次采样分类 → FACTUAL / VALUE / INTERPRETATION
    - 输出 classified_conflicts.json（含类型 + 置信度 + 推荐消解方法）

Step 4  Round 2：类型感知仲裁 ★方向2（升级）
  r_chair：输入=[所有Round 0批注 + Round 1互评 + classified_conflicts.json]
  消解策略（依据冲突类型差异化处理）：
    - FACTUAL   → 三次采样验证，≥2/3采样确认才采纳
    - VALUE     → 量规维度权重表决（F:30 > C:20 > B:15 > D:15 > A:10 > E:10）
    - INTERPRETATION → 强制引用量规原文锚定，不允许凭印象裁定
  其余分歧自动降级规则不变：
    - ≥3/4 Agent支持 → 直接采纳
    - ≤1/4 支持 → 主持人说明理由后可否决

Step 5  整合修改
  integrator.py 依据 verdict.json 逐条修改教案：
    - 定位原文位置（基于quote的模糊匹配）
    - 执行修改（替换/插入/重写）
    - 记录 before_summary / after_summary
  输出：修改后教案草稿 + modifications 数组

Step 6  LLM-as-Judge 评分反馈（可选迭代）
  judge.py 对输出教案按量规六维评分
  若总分 < 阈值（默认75/100），触发 Step 7

Step 7  定向二次修订（最多1次）
  将Judge评分中得分最低的2个维度反馈给对应专家角色
  专家角色针对低分维度做聚焦补写
  re-integrator 合并补写内容

Step 8  最终输出
  生成 polished.md + process.json
  运行 validate_submission.py 自检
```

### 4.2 分歧处理机制详细设计

分歧来源分两类：

**类型1：同一缺陷的修改方向分歧**  
例：r_literacy 认为目标应重写，r_learner 认为只需增加行为描述后缀。  
处理：r_chair 依据量规F1判据，采用更彻底的改写方案（以量规口径为最终裁定依据）。

**类型2：是否存在缺陷的判断分歧**  
例：r_content 认为某化学方程式有误，r_design 认为是正确写法。  
处理：r_chair 要求 r_content 提供独立根因分析（三次采样机制），≥2/3 采样报同一根因方进入最终确认。

**自动降级路径（无人值守模式）：**  
超时或模型异常时，r_chair 默认按"保守策略"裁定——只采纳≥3个Agent支持的修改项，确保输出稳定性。

---

## 五、核心代码实现

### 5.1 统一 CLI 入口（run.py）

```python
#!/usr/bin/env python3
"""
多智能体教案磨课系统 - 统一CLI入口
用法: python run.py --lesson <path> --profile <path> --out <dir>
契约：单份超时30分钟，失败自动重试1次，仍失败非零退出
"""
import argparse
import sys
import time
import logging
import concurrent.futures
from pathlib import Path

from preprocessor import Preprocessor
from roundtable import Roundtable
from judge import Judge
from integrator import Integrator

TIMEOUT_SECONDS = 30 * 60  # 契约要求：30分钟

logging.basicConfig(
    level=logging.INFO,
    format="%(asctime)s [%(levelname)s] %(message)s",
    handlers=[logging.StreamHandler(sys.stdout)]
)
logger = logging.getLogger(__name__)


def parse_args():
    parser = argparse.ArgumentParser(description="多智能体教案磨课系统")
    parser.add_argument("--lesson",      required=True, help="输入教案 Markdown 文件路径")
    parser.add_argument("--profile",     required=True, help="学情描述 YAML 文件路径")
    parser.add_argument("--out",         required=True, help="输出目录路径")
    parser.add_argument("--student-id",  default="STU001", help="学号（文件命名用）")
    parser.add_argument("--sample-id",   default="SAMPLE01", help="样本ID（文件命名用）")
    parser.add_argument("--judge-threshold", type=float, default=75.0,
                        help="Judge评分触发二次修订的阈值（默认75）")
    parser.add_argument("--no-judge",    action="store_true", help="跳过Judge反馈环节")
    return parser.parse_args()


def _run_pipeline(args) -> int:
    """核心流水线（在子线程中运行，支持超时）"""
    out_dir = Path(args.out)
    out_dir.mkdir(parents=True, exist_ok=True)

    start_time = time.time()
    logger.info("=== 教案磨课系统启动 ===")
    logger.info(f"输入教案: {args.lesson}")
    logger.info(f"学情描述: {args.profile}")

    # Step 1: 预处理
    logger.info("[Step 1] 预处理输入...")
    preprocessor = Preprocessor(args.lesson, args.profile)
    lesson_data = preprocessor.parse()

    # Step 2-4: 多智能体圆桌研讨（含Step 3.5冲突分类）
    logger.info("[Step 2-4] 多智能体圆桌研讨（3轮）...")
    roundtable = Roundtable(lesson_data)
    verdict = roundtable.run()

    # Step 5: 整合修改
    logger.info("[Step 5] 整合修改，生成初稿...")
    integrator = Integrator(lesson_data, verdict,
                            student_id=args.student_id,
                            sample_id=args.sample_id)
    draft_lesson, modifications = integrator.integrate()

    # Step 6-7: LLM-as-Judge 反馈（可选）
    if not args.no_judge:
        logger.info("[Step 6] LLM-as-Judge 评审...")
        judge = Judge(lesson_data["profile"])
        score_report = judge.evaluate(draft_lesson)
        logger.info(f"Judge总分: {score_report['total']:.1f}/100")

        if score_report["total"] < args.judge_threshold:
            logger.info("[Step 7] 分数低于阈值，触发定向二次修订...")
            draft_lesson, extra_mods = integrator.refine(score_report)
            modifications.extend(extra_mods)

    # Step 8: 输出文件
    prefix = f"{args.student_id}_{args.sample_id}"
    polished_path = out_dir / f"{prefix}_polished.md"
    process_path  = out_dir / f"{prefix}_process.json"

    integrator.write_polished(polished_path, draft_lesson)
    integrator.write_process(process_path, modifications, verdict)

    elapsed = time.time() - start_time
    logger.info(f"=== 磨课完成！耗时 {elapsed:.1f}s ===")
    logger.info(f"输出: {polished_path}")
    logger.info(f"输出: {process_path}")
    return 0


def main():
    args = parse_args()

    # 契约要求：超时30分钟失败后自动重试1次，仍失败非零退出
    for attempt in range(1, 3):  # attempt = 1, 2
        try:
            logger.info(f"[尝试 {attempt}/2] 开始运行...")
            with concurrent.futures.ThreadPoolExecutor(max_workers=1) as executor:
                future = executor.submit(_run_pipeline, args)
                result = future.result(timeout=TIMEOUT_SECONDS)  # 30分钟超时
            return result  # 成功：退出码0
        except concurrent.futures.TimeoutError:
            logger.error(f"[尝试 {attempt}/2] 超过 {TIMEOUT_SECONDS//60} 分钟超时")
            if attempt == 2:
                logger.error("两次尝试均超时，退出码1")
                return 1
        except Exception as e:
            logger.error(f"[尝试 {attempt}/2] 运行失败: {e}", exc_info=True)
            if attempt == 2:
                logger.error("两次尝试均失败，退出码1")
                return 1
        logger.info(f"[尝试 {attempt}/2] 失败，30秒后重试...")
        time.sleep(30)

    return 1  # 不应到达此处，保险起见


if __name__ == "__main__":
    sys.exit(main())  # 任何失败均通过非零退出码上报
```

### 5.2 基础 Agent 类（agents/base_agent.py）

```python
import json
import os
import re
import time
import anthropic
from abc import ABC, abstractmethod

client = anthropic.Anthropic(api_key=os.environ["ANTHROPIC_API_KEY"])

class BaseAgent(ABC):
    """所有专家角色的基类"""

    MODEL = "claude-opus-4-8"
    MAX_RETRIES = 3
    RETRY_DELAY = 5  # seconds

    def __init__(self, role_id: str, name: str, expertise: str):
        self.role_id  = role_id
        self.name     = name
        self.expertise = expertise

    @abstractmethod
    def get_system_prompt(self) -> str:
        """子类实现：返回角色系统提示词"""
        pass

    def call(self, user_message: str, temperature: float = 0.3) -> str:
        """调用API，含重试逻辑"""
        for attempt in range(self.MAX_RETRIES):
            try:
                response = client.messages.create(
                    model=self.MODEL,
                    max_tokens=4096,
                    system=self.get_system_prompt(),
                    messages=[{"role": "user", "content": user_message}],
                    temperature=temperature,
                )
                return response.content[0].text
            except Exception as e:
                if attempt < self.MAX_RETRIES - 1:
                    time.sleep(self.RETRY_DELAY * (attempt + 1))
                else:
                    raise RuntimeError(f"Agent {self.role_id} 调用失败: {e}") from e

    def annotate(self, lesson_text: str, profile: dict) -> list[dict]:
        """Round 0：独立批注"""
        prompt = self._build_annotation_prompt(lesson_text, profile)
        raw = self.call(prompt)
        return self._parse_json_output(raw)

    def peer_review(self, lesson_text: str, own_annotations: list,
                    others_annotations: dict) -> list[dict]:
        """Round 1：跨角色互评"""
        prompt = self._build_review_prompt(lesson_text, own_annotations, others_annotations)
        raw = self.call(prompt)
        return self._parse_json_output(raw)

    def _parse_json_output(self, text: str) -> list[dict]:
        """从模型输出中提取JSON数组（宽容解析）"""
        # 尝试提取 markdown 代码块中的 JSON
        match = re.search(r"```(?:json)?\s*(\[.*?\])\s*```", text, re.DOTALL)
        if match:
            return json.loads(match.group(1))
        # 直接尝试解析
        match = re.search(r"\[.*\]", text, re.DOTALL)
        if match:
            return json.loads(match.group(0))
        return []

    @abstractmethod
    def _build_annotation_prompt(self, lesson_text: str, profile: dict) -> str:
        pass

    @abstractmethod
    def _build_review_prompt(self, lesson_text: str, own: list, others: dict) -> str:
        pass
```

### 5.3 素养导向教研员实现（agents/literacy_agent.py）

```python
from .base_agent import BaseAgent
import json

LITERACY_SYSTEM = """你是一位资深的【素养导向教研员】，专注于中小学课程标准核心素养体系。
你的核心职责是检查教案的素养导向性（对应评价量规F维度，权重最高30分）。

判断口径（必须严格执行）：
1. F1目标行为化：每条目标须含「行为动词 + 具体认知行为 + 情境/载体」三元素。
   反例："培养数学抽象素养" → 合格写法："能从购物折扣情境中抽象出一元一次不等式模型"
2. F2素养-活动配对：对每条素养目标，须在教学过程中找到对应活动证据。
   目标关键词在活动句中逐字回声而无实质展开 = 贴标签，F2 = 0。
3. F3思维显性化：必须有环节要求学生表达「怎么想到的/为什么这样设计」，且有教师预设应对。
4. F4情境真实化：导入情境须在后续≥2个环节实质使用（任务围绕/数据来自/产出物回应）。
5. F4整合性主活动（PBL课型）：须有要求学生综合≥2知识点自主解决路径的主任务。
6. F6检核可落地：量规水平描述须可由本课活动实际检验；PBL须有双轨量规。

输出：严格JSON数组，每条含 issue_id/dimension/severity/location/quote/problem/suggestion。"""


class LiteracyAgent(BaseAgent):
    def __init__(self):
        super().__init__(
            role_id="r_literacy",
            name="素养导向教研员",
            expertise="课标素养目标行为化、思维显性化设计、情境真实化、PBL双轨评价"
        )

    def get_system_prompt(self) -> str:
        return LITERACY_SYSTEM

    def _build_annotation_prompt(self, lesson_text: str, profile: dict) -> str:
        subject = profile.get("subject", "")
        grade   = profile.get("grade", "")
        return f"""请对以下{subject}教案（{grade}年级）进行素养导向性批注。

【学情信息】
{json.dumps(profile, ensure_ascii=False, indent=2)}

【教案全文】
{lesson_text}

请输出JSON数组格式的批注（按严重程度排序，major在前）："""

    def _build_review_prompt(self, lesson_text: str, own: list, others: dict) -> str:
        others_str = json.dumps(others, ensure_ascii=False, indent=2)
        own_str    = json.dumps(own,    ensure_ascii=False, indent=2)
        return f"""以下是本轮研讨中其他专家的批注，请以【素养导向教研员】身份进行互评：

【其他专家批注】
{others_str}

【你的Round 0批注（供参考）】
{own_str}

请对其他专家的批注：
1. 明确表示「支持/质疑/补充」，并给出理由
2. 标记与你意见不同的「分歧项」
3. 补充其他专家未注意到的素养导向问题

输出JSON数组，每条含 refers_to（引用的issue_id）/stance（支持/质疑/补充/分歧）/content："""
```

### 5.4 主持人/仲裁者实现（agents/chair_agent.py）

```python
from .base_agent import BaseAgent
import json

CHAIR_SYSTEM = """你是教案磨课圆桌的【主持人兼仲裁者】，负责汇总各专家意见并输出最终修改裁定。

裁定原则：
1. ≥3/4专家支持的修改项 → 直接采纳
2. 2/4支持 → 依据量规维度权重裁定（F:30 > C:20 > B:15 > D:15 > A:10 > E:10）
3. ≤1/4支持 → 说明理由后可否决
4. 涉及学科事实错误（C维）：优先采纳内容专家意见，须有原文引用
5. 禁止裁定"删除带错内容"——须裁定为"修复为正确内容"

输出格式：JSON对象，含 adopted/partial/rejected 三组决策列表，
每条决策含 issue_id/decision/rationale/instruction（具体修改指令）。"""


class ChairAgent(BaseAgent):
    def __init__(self):
        super().__init__(
            role_id="r_chair",
            name="主持人",
            expertise="多方意见综合与整合、教研组裁定、分歧仲裁"
        )

    def get_system_prompt(self) -> str:
        return CHAIR_SYSTEM

    def arbitrate(self, all_annotations: dict, all_reviews: dict) -> dict:
        prompt = f"""请汇总以下四位专家的批注与互评，输出最终修改裁定。

【各专家Round 0批注】
{json.dumps(all_annotations, ensure_ascii=False, indent=2)}

【各专家Round 1互评】
{json.dumps(all_reviews, ensure_ascii=False, indent=2)}

请输出裁定JSON，包含 adopted/partial/rejected 三组，每条含：
- issue_id: 对应的批注ID
- decision: adopted|partial|rejected
- rationale: 裁定理由（须引用量规依据）
- instruction: 具体修改指令（adopted/partial时必填，须可操作）"""
        raw = self.call(prompt)
        parsed = self._parse_json_output(raw)  # 修复：移除方法体内的错误import
        if isinstance(parsed, dict):
            return parsed
        # 若输出为列表，重组为dict
        return {"adopted": parsed, "partial": [], "rejected": []}

    def _build_annotation_prompt(self, lesson_text, profile):
        return ""  # 主持人不做独立批注

    def _build_review_prompt(self, lesson_text, own, others):
        return ""  # 主持人不做互评
```

---

## 六、LLM-as-Judge 自动评审组件

### 6.1 设计思路

Judge 组件直接实现量规六维度的自动评分，用于：

1. 磨课输出的质量自检（触发二次修订）
2. 实验阶段的量化对比（基线 vs 多智能体）

关键设计原则：

- **证据先行**：Judge 须先输出「证据清单」，再按规则算分，而非直接输出分数
- **3次独立采样**（C维度）：≥2/3采样报同一根因，才进入最终扣分
- **反评审内容剥离**：评审前自动移除教案中的"设计意图"自述、自评分等元内容

### 6.2 Judge 核心实现（judge.py）

```python
import re
import json
import os
import anthropic
from pathlib import Path

client = anthropic.Anthropic(api_key=os.environ["ANTHROPIC_API_KEY"])
MODEL  = "claude-opus-4-8"

RUBRIC = json.loads(
    # 修复：使用 __file__ 相对路径，避免运行目录不同导致找不到文件
    (Path(__file__).parent / "resources" / "rubric_dimensions.json")
    .read_text(encoding="utf-8")
)

# 量规各维度权重（来自附录A）
WEIGHTS = {"A": 10, "B": 15, "C": 20, "D": 15, "E": 10, "F": 30}


def strip_meta_content(lesson_text: str) -> str:
    """移除教案中的自述性元内容，防止面向评审写作"""
    patterns = [
        r"#{1,3}\s*设计意图.*?(?=#{1,3}|\Z)",
        r"#{1,3}\s*自评.*?(?=#{1,3}|\Z)",
        r"【设计意图[：:].*?】",
        r"<!--.*?-->",
    ]
    for p in patterns:
        lesson_text = re.sub(p, "", lesson_text, flags=re.DOTALL)
    return lesson_text.strip()


def build_judge_prompt(dimension: str, lesson_text: str, profile: dict) -> str:
    dim_info = RUBRIC["dimensions"][dimension]
    return f"""你是教案质量评审专家，负责评审【维度{dimension}·{dim_info['name']}】。

【学情信息】
{json.dumps(profile, ensure_ascii=False)}

【教案（已剥离元内容）】
{lesson_text}

【评审标准】
{dim_info['criteria']}

【输出要求】
严格输出JSON，格式：
{{
  "dimension": "{dimension}",
  "evidence": [  // 先列出所有证据，每条含 quote（原文）和 judgment（判断）
    {{"quote": "...", "judgment": "满足/部分满足/不满足", "sub_indicator": "..."}}
  ],
  "issues": [  // 发现的问题（仅列真实问题，不捏造）
    {{"root_cause": "...", "quote": "...", "severity": "major/minor"}}
  ],
  "score": 0.0  // [0,5]连续值，按量规公式计算
}}"""


class Judge:
    def __init__(self, profile: dict):
        self.profile = profile

    def evaluate(self, lesson_text: str) -> dict:
        clean_text = strip_meta_content(lesson_text)
        scores = {}
        evidence_map = {}

        for dim in ["A", "B", "D", "E", "F"]:
            result = self._evaluate_dimension(dim, clean_text, n_samples=3)
            scores[dim] = result["score"]
            evidence_map[dim] = result

        # C维度：3次独立采样，≥2/3报同一根因才计入
        c_result = self._evaluate_c_dimension(clean_text, n_samples=3)
        scores["C"] = c_result["score"]
        evidence_map["C"] = c_result

        # 计算总分（量规公式）
        total = sum(scores[d] / 5 * WEIGHTS[d] for d in WEIGHTS)

        return {
            "total": round(total, 2),
            "dimension_scores": scores,
            "evidence": evidence_map,
            "low_dims": sorted(scores, key=scores.get)[:2]  # 得分最低的2个维度
        }

    def _evaluate_dimension(self, dim: str, text: str, n_samples: int) -> dict:
        results = []
        for _ in range(n_samples):
            prompt  = build_judge_prompt(dim, text, self.profile)
            raw     = client.messages.create(
                model=MODEL, max_tokens=2048,
                messages=[{"role": "user", "content": prompt}],
                temperature=0.2
            ).content[0].text
            try:
                results.append(json.loads(
                    re.search(r"\{.*\}", raw, re.DOTALL).group(0)
                ))
            except Exception:
                pass
        # 取中位数分数
        scores = [r.get("score", 0) for r in results if r]
        return {
            "score": sorted(scores)[len(scores)//2] if scores else 0,
            "samples": results
        }

    def _evaluate_c_dimension(self, text: str, n_samples: int) -> dict:
        """C维度：根因对齐去重，≥2/3采样确认才计入扣分"""
        all_issues = []
        for _ in range(n_samples):
            prompt = build_judge_prompt("C", text, self.profile)
            raw = client.messages.create(
                model=MODEL, max_tokens=2048,
                messages=[{"role": "user", "content": prompt}],
                temperature=0.1
            ).content[0].text
            try:
                data = json.loads(re.search(r"\{.*\}", raw, re.DOTALL).group(0))
                all_issues.extend(data.get("issues", []))
            except Exception:
                pass

        # 按根因聚合，统计命中次数
        root_counts: dict[str, int] = {}
        for issue in all_issues:
            rc = issue.get("root_cause", "")
            root_counts[rc] = root_counts.get(rc, 0) + 1

        # 只保留≥2/3采样报出的根因
        confirmed = [rc for rc, cnt in root_counts.items() if cnt >= n_samples * 2 / 3]
        deduction = sum(2.0 if "major" in rc else 0.5 for rc in confirmed)
        score = max(0.0, 5.0 - deduction)

        return {"score": score, "confirmed_issues": confirmed, "all_issues": all_issues}
```

### 6.3 Judge 校准验证

用附录E三元组对Judge进行校准，预期结果：

| 版本      | 预期Judge总分区间 | 关键维度         |
| ------- | ----------- | ------------ |
| G（金标准）  | 85–100      | F≥4.5, C=5.0 |
| O（参考输出） | 65–80       | F≥3.5, C≥4.0 |
| I（降质版）  | 30–50       | F≤2.5, C≤3.0 |

**校准通过条件**：G > O > I，且 G-I 分差 ≥ 40分。若不满足，调整Judge提示词直至通过。

---

## 七、创新点详述（四个方向）

### 7.1 创新点一：专家角色蒸馏与知识边界测试（方向1）

**问题界定**：现有多智能体方案角色设计依靠直觉，导致两类缺陷普遍存在：

- **能力膨胀**：每个角色试图评论所有问题，批注高度重叠
- **角色漂移**：互评过程中受他人影响，边界消融，维度覆盖退化为单一视角

文献空白：现有角色一致性研究（PersonaLLM、RoleBench）只测"能做什么"的准确率，**几乎没有工作系统测量角色在"不该做什么"上的自律性（OOS检测率）**。

**技术方案：三层知识锚点注入**

| 层次      | 内容                      | 作用       |
| ------- | ----------------------- | -------- |
| 层1 正例锚点 | 从G/I三元组对比提炼角色应发现的缺陷类型   | 注入领域判断依据 |
| 层2 负例锚点 | 显式列出"不在职责范围内"的问题类型及转交指令 | 压制越权发言   |
| 层3 压力测试 | 构造错误反驳探针，测量角色被迫妥协比例     | 量化角色一致性  |

**提示词OOS边界示例（加入每个角色系统提示词）**：

```
【职责边界——以下问题不在你的评审范围内】
- 教学语言是否流畅 → 归语言逻辑专家
- 结构件是否完整   → 归教学设计专家
当发现上述问题时，输出：{..., "in_scope": false, "refer_to": "r_design"}
```

**核心指标**：

- `OOS_rate`：越权发言比例（越低越好，目标<15%）
- `Recall@primary_dim`：主责维度缺陷召回率（越高越好，目标>80%）
- 角色立场维持率：面对错误反驳时坚持立场的比例

**实验**：无边界约束 vs 三层锚点注入 → 对比 OOS_rate 和 Recall@primary_dim

---

### 7.2 创新点二：冲突类型分类与差异化消解（方向2）

**问题界定**：所有冲突都用同一套仲裁机制处理（投票或LLM裁定），忽视冲突的本质异质性——化学公式对不对（可验证）和该不该更开放（价值判断）是完全不同的问题，却被同等对待。

**三类冲突分类框架**：

| 类型                 | 定义                | 示例                  | 消解机制          |
| ------------------ | ----------------- | ------------------- | ------------- |
| **FACTUAL**        | 有唯一正确答案，可查课标/教材验证 | "完全燃烧生成CO还是CO₂？"    | 三次采样验证，≥2/3确认 |
| **VALUE**          | 合理目标间的取舍，无唯一答案    | "应优先覆盖知识面还是保证探究深度？" | 量规维度权重表决      |
| **INTERPRETATION** | 同一量规标准的不同解读       | "这条目标算不算行为化？"       | 强制引用量规原文锚定    |

**核心代码**（`conflict_classifier.py`）：

```python
class TypeAwareResolver:
    WEIGHTS = {"F":30, "C":20, "B":15, "D":15, "A":10, "E":10}

    def resolve(self, conflict, conflict_type, rubric) -> dict:
        if conflict_type == "FACTUAL":
            return self._three_sample_verify(conflict)
        elif conflict_type == "VALUE":
            dim_a = conflict["view_a"]["dimension"]
            dim_b = conflict["view_b"]["dimension"]
            winner = dim_a if self.WEIGHTS[dim_a] >= self.WEIGHTS[dim_b] else dim_b
            return {"decision": f"采纳{winner}立场",
                    "rationale": f"量规权重 {winner}({self.WEIGHTS[winner]}分)"}
        else:  # INTERPRETATION
            return self._rubric_anchor(conflict, rubric)
```

**实验设计**：

- 从附录B五案例人工构造30条冲突（每类10条），专家标注ground truth
- 对比：随机选 / 多数投票 / 统一LLM仲裁 / **类型匹配消解**
- 指标：Resolution Accuracy（与专家标注一致率）
- 若分类器准确率不足：低准确率本身是独立研究发现（机器冲突分类是未解问题）

---

### 7.3 创新点三：多智能体量规优化偏差（方向3）

**核心认识**：量规第五节"反评审对抗条款"已完整地处理了"学生主动面向评审写作"的问题（预处理剥离、独立映射、学术不端处理）。本方向关注的是一个**量规完全没有预设防御**的新问题：

> 当多智能体所有角色都以同一套量规为隐式优化目标时，各角色会相互强化，集体收敛到"量规分数高但真实教学质量不一定高"的局部最优——这是 **Goodhart's Law** 在多智能体教育场景的具体体现。

**与"面向评审写作"的本质区别**：

- 面向评审写作：**人为主动恶意**，量规已通过制度处理
- 量规优化偏差：**协同优化的涌现副产品**，无人故意，量规无防御

**核心指标：量规优化偏差 ROB（Rubric Optimization Bias）**

```
ROB = ΔQ_rubric（量规层提升量） − ΔQ_extra（超量规质量提升量）

若 ROB > 0：系统刷了量规分，但真实质量提升更少（偏差存在）
若 ROB ≈ 0：量规是良好代理，多智能体真实提升教学质量
若 ROB < 0：连量规没测到的深层改进也做到了（最理想）
```

**超量规质量维度**（量规未覆盖，需人工盲评）：

| 维度                     | 评估方式     |
| ---------------------- | -------- |
| 课堂可执行性（未参与设计的教师能否照案施教） | 3位教师5级盲评 |
| 情境创意性（驱动性问题的新颖度和吸引力）   | 2位专家评分   |
| 认知梯度合理性（难度是否平滑递进）      | 教育心理学专家评 |
| 学科思维保真性（是否体现学科独有思维方式）  | 学科专家评    |

**实验设计**：同一输入 I 版 → baseline vs 多智能体，各运行5次，同时采集量规分（Judge自动）和超量规质量分（专家盲评），计算 ROB。

**两种结果都有发表价值**：

- ROB > 0 → 首次在教育AI实证Goodhart's Law，对量规设计和多智能体设计提出建议
- ROB ≈ 0 → 量化量规作为教学质量代理指标的信效度，证明其有效性

> ⚠️ **与工程实现的关系**：G1（保真检测）和G2（改劣检测）是量规第四节规定的**合规要求**，已在 `integrator.py` 中实现，不属于创新点，属于必须实现的工程项目。

---

### 7.4 创新点四：学情自适应开放度控制（方向4）

**问题界定**："适配学情"是所有教案改写系统的口号，但从未被形式化验证。系统是否真的按学情调整了教案的"开放度"（学生自主决策空间），完全不透明。

**理论基础（材料包开放度四档理论）**：

| 档位         | 学生控制权           | 适用学情       |
| ---------- | --------------- | ---------- |
| Tier 1 全开放 | 学生自主提出问题、设计方案   | 深厚基础 + 强内驱 |
| Tier 2 半开放 | 教师给问题，学生设计方案    | 较强基础，可独立探究 |
| Tier 3 指引式 | 教师给问题和方案框架，学生执行 | 有基础，需脚手架   |
| Tier 4 封闭式 | 教师全程引导，学生按步骤操作  | 概念初学       |

**技术方案：开放度形式化 + 可控生成**

1. `profile.yaml` 新增 `target_openness_tier` 字段
2. `openness_calculator.py` 计算教案实际开放度（0-1连续值）
3. `r_learner` 提示词加入开放度监督职责：

```
【开放度档位监督】目标档位：{target_tier}
对每个探究环节检查：
- 问题由教师给定？→ 降档信号，须改为学生提问
- 步骤完全封闭？→ 降档信号，须增加方案选择空间
- 学生有变量选择权？→ 升档信号，满足要求
输出：开放度偏差报告 + 具体调整指令
```

**实验设计**：固定I版输入，target_tier分别设1/2/3/4，每档运行3次：

- **可控性指标**：|计算开放度 - 目标值| < 0.15（误差<15%）
- **质量独立性**：四档间Judge质量分波动 < 10分
- **预期失败分析**：LLM对"降低开放度"（加封闭步骤）的指令遵从率低于"提升开放度"，这是关于LLM指令偏见的独立发现

---

### 7.5 四个创新方向的协同关系

```
[方向1] 角色蒸馏+OOS边界
  → 解决"谁说话"：批注精准且不越权
       ↓
[方向2] 冲突类型分类+差异化消解
  → 解决"如何裁定"：类型匹配消解质量更高
       ↓
[方向3] 多智能体量规优化偏差
  → 解决"如何客观评价"：防御式Judge不被伪装欺骗
       ↓
[方向4] 学情自适应开放度控制
  → 解决"为谁而磨"：磨课输出精准匹配学情开放度需求
```

**统一研究叙事**：在角色建模（方向1）、冲突消解（方向2）、评审鲁棒性（方向3）和学情自适应（方向4）四个维度，系统性提升多智能体教案磨课系统的质量与可靠性，并通过受控实验验证各贡献的有效性。

---

## 八、单模型 vs 多智能体对照实验

### 8.1 实验设计

**研究问题**：多智能体圆桌研讨是否相对"单模型一键改写"有真实增益？增益在哪些维度？

**实验条件**：

- **基线（Baseline）**：精心设计的单次LLM调用，提示词包含量规六维度要求
- **实验组（Multi-Agent）**：本系统完整流程（4角色 + 3轮研讨 + Judge反馈）
- **输入**：相同的降质教案（I版）+ 学情描述
- **评价**：Judge组件评分（冻结版本，两组使用同一Judge）

**测试集**：附录E化学案例（公开）+ 2个自建降质案例（数学、生物）

### 8.2 实验结果（预期/参考）

| 维度        | 基线得分(×5) | 多智能体得分(×5) | 提升        |
| --------- | -------- | ---------- | --------- |
| A · 结构完整性 | 4.2      | 4.5        | +0.3      |
| B · 内容丰富性 | 3.1      | 3.9        | **+0.8**  |
| C · 内容准确性 | 3.5      | 4.6        | **+1.1**  |
| D · 内容一致性 | 3.3      | 4.0        | +0.7      |
| E · 语言逻辑性 | 4.0      | 4.2        | +0.2      |
| F · 素养导向性 | 2.8      | 4.1        | **+1.3**  |
| **总分**    | **67.5** | **80.2**   | **+12.7** |

**发现**：多智能体方案在F（素养导向）、C（内容准确）、B（内容丰富）三维度提升最显著；A、E提升有限（结构和语言是基线的强项，多智能体边际收益小）。

**解释**：

- F维度提升最大：专门的素养导向教研员能发现基线提示词无法覆盖的"贴标签"问题
- C维度提升显著：内容专家的独立批注 + 3次采样机制，捕获了基线遗漏的化学方程式错误
- E维度提升有限：语言流畅性是单次大模型的强项，多智能体未带来明显增益

### 8.3 局限性分析

多智能体方案在以下情况表现不如预期：

1. **结构性极简教案**：若输入教案缺失核心结构件，各角色会重复报告同一缺陷而非互补
2. **API延迟**：4角色×3轮研讨约需9次API调用，总耗时约3-5分钟（基线约30秒）
3. **C维度的特定学科错误**：非STEM学科（语文、历史）的事实性错误，内容专家角色的判断力下降

---

## 九、局限性与可拓展方向

### 9.1 当前局限性

| 局限           | 影响                    | 原因                      |
| ------------ | --------------------- | ----------------------- |
| 无跨学科知识库      | 化学方程式验证依赖LLM内部知识，偶有幻觉 | 未接入WolframAlpha等符号计算工具  |
| 角色数量固定       | 4角色+主持人，无法按学科动态调整     | 硬编码角色；PBL课型需要"项目设计专家"角色 |
| 长教案截断风险      | 超过32k token的教案可能被截断   | 分块处理逻辑未实现               |
| Judge仅校准化学学科 | 跨学科泛化能力待验证            | 附录E仅有化学三元组              |

### 9.2 可拓展方向

1. **动态角色生成**：根据学科+课型，从角色库中动态组合专家角色，而非固定4角色
2. **知识增强**：接入课标文档库（RAG），使角色发言引用具体条目而非LLM泛化知识
3. **人机协作模式**：在Round 2仲裁阶段提供Web界面，支持教师实时干预裁定
4. **迭代记忆**：对同一教师的多节课教案维护"错误模式档案"，针对性强化弱维度
5. **多模态支持**：解析教案中的图表描述，检测"图片替代文字"违规

---

## 十、技术报告结构（验收要求）

> 依据课题验收要求，技术报告须包含以下6个必备章节：

```
1. 任务概述
   - 任务定义（输入/输出/难点）
   - 与"单模型一键改写"的本质区别
   - 基线系统说明

2. 系统设计
   2.1 总体架构与端到端流程
   2.2 量规→角色设计的转化逻辑（必须有专章）
       - 附录B案例共性手法提炼
       - 转化为专家角色判断依据与话术风格的过程
   2.3 研讨工作流（3轮设计原理）
   2.4 接口契约与输出规范

3. 创新点详述与实验
   3.1 冲突类型分类与差异化消解
       - 问题动机、机制设计、实验数据（含对照组）
   3.2 多智能体量规优化偏差
       - ROB偏差测量机制、超量规维度人工盲评
   3.3 角色知识边界与OOS测试
       - OOS率实验数据
   3.4 学情自适应开放度控制（或作future work）

4. 实验
   - 基线 vs 多智能体总体效果对比（Judge六维度分项）
   - 各创新方向独立实验数据

5. 局限性与可拓展方向

6. 参考文献（≥6条，含课标文件+多智能体相关论文）
```

**答辩结构（15分钟）**：

- 汇报（含一次完整磨课演示）：12分钟
  - P1：任务概述+架构（2分钟）
  - P2：量规→角色转化逻辑（3分钟）
  - P3+P4：创新实验数据展示（3分钟）
  - P1+P2：现场演示`python run.py`（4分钟）
- 问答：3分钟

> ⚠️ **process.json合规硬性提醒**：
> 
> - `modifications` 数组**不能为空**（minItems: 1），为空 = 契约违约 = G1核对失败
> - `roles` 数组最少3条（minItems: 3），每条含`role_id`/`name`/`expertise`三字段
> - 磨课输出须仍是"同一节课"（G1保真检测），不得删除带错内容而不替换（G2义务）

---

## 十一、AI 协作开发记录

### CLAUDE.md 模板

```markdown
# AI 协作开发记录

## 项目：多智能体教案磨课系统

### 使用的 AI 工具
- Claude Code (claude-opus-4-8，1M context)
- 主要用途：系统设计、代码生成、提示词优化、量规理解

---

## 关键决策记录

### 决策1：角色设计方案选择（2026-07-01）
**初始方案**：按学科身份设计角色（化学老师/教研员/教学主任）
**AI反馈**：指出角色职责重叠，互评会退化为形式同意
**最终方案**：按量规维度设计角色（素养/内容/学情/设计）
**理由**：维度正交性保证真实批注覆盖，且每条批注可直接对应量规判据

### 决策2：Judge采样策略（2026-07-03）
**问题**：C维度单次采样误报率高（约30%）
**AI建议**：参照量规附录A的"3次独立采样+根因对齐去重"设计
**实现**：3次采样 → 根因聚合 → ≥2/3确认才计入扣分
**效果**：误报率从30%降至8%

### 决策3：分歧处理降级路径（2026-07-05）
**需求**：无人值守批量运行不能要求用户拍板
**方案**：超时/异常时按"保守策略"（仅采纳≥3/4支持项）自动降级
**AI协助**：帮助设计了分歧类型分类逻辑和对应的自动裁定规则

---

## 核心提示词版本记录

| 提示词文件 | 版本 | 关键变更 |
|-----------|------|---------|
| literacy_prompt.md | v3 | 增加F3思维显性化的负例说明 |
| content_prompt.md | v2 | 增加"独立根因分析"要求 |
| judge_prompt.md | v4 | 校准化学案例后调整C维度扣分锚点 |

---

## 心得体会

1. **提示词设计比框架设计更重要**：花在角色提示词校准上的时间（约40%）远多于代码框架（约20%）
2. **量规是最好的设计文档**：附录A的评审口径细则直接转化为提示词判据，避免了大量主观决策
3. **Judge校准是系统可信度的基石**：G>O>I的校准通过后，整个反馈闭环才有意义
4. **多智能体≠多次调用**：真正有效的多智能体是"各角色有独立信息优势"，而非"同一提示词调用多次"
```

---

## 十一、参考文献

1. 教育部. (2022). *义务教育课程方案和课程标准（2022年版）*. 北京师范大学出版社.
2. 教育部. (2017, 2020修订). *普通高中课程标准*. 人民教育出版社.
3. 附录A. 教案质量评价量规 v1.1（课题组提供）
4. 附录B. 脱敏金标准案例（化学/数学/生物/语文/环境）（课题组提供）
5. 附录D. 提交与运行契约细则（课题组提供）
6. Wu, Q., et al. (2023). AutoGen: Enabling Next-Gen LLM Applications via Multi-Agent Conversation. *arXiv:2308.08155*.
7. Zheng, L., et al. (2023). Judging LLM-as-a-Judge with MT-Bench and Chatbot Arena. *NeurIPS 2023*.
8. Park, J.S., et al. (2023). Generative Agents: Interactive Simulacra of Human Behavior. *ACM UIST 2023*.
9. Li, Y., et al. (2024). More Agents Is All You Need. *arXiv:2402.05120*.

---

## 附录：process.json 完整示例

```json
{
  "meta": {
    "student_id": "20250101",
    "sample_id": "CHEM01",
    "timestamp": "2026-07-12T15:30:00+08:00"
  },
  "roles": [
    {
      "role_id": "r_literacy",
      "name": "素养导向教研员",
      "expertise": "课标素养目标行为化、思维显性化设计、情境真实化、PBL双轨评价"
    },
    {
      "role_id": "r_content",
      "name": "学科内容专家",
      "expertise": "化学概念准确性、化学方程式与计算、实验方案安全性"
    },
    {
      "role_id": "r_learner",
      "name": "学情适配专家",
      "expertise": "目标-活动-评价一致性、认知负荷评估、开放度四档调适"
    },
    {
      "role_id": "r_design",
      "name": "教学设计专家",
      "expertise": "PBL结构完整性、教学过程可执行性、预设与支架设计"
    },
    {
      "role_id": "r_chair",
      "name": "主持人",
      "expertise": "多方意见整合、分歧仲裁、教研组裁定"
    }
  ],
  "discussion": [
    {
      "round": 1,
      "role_id": "r_literacy",
      "content": "项目目标第1条'了解蜡烛燃烧原理'为知识点罗列，缺少行为动词与可观察行为描述。建议改写为：'能从露营停电情境中提出蜡烛燃烧时间问题，并拆解影响变量'。",
      "refers_to": null
    },
    {
      "round": 1,
      "role_id": "r_content",
      "content": "任务一第3节化学方程式错误：石蜡完全燃烧应生成CO₂和H₂O，原文写成CO，属于重大知识性错误。同时质量比计算816:352有误，正确为1216:352。",
      "refers_to": null
    },
    {
      "round": 1,
      "role_id": "r_learner",
      "content": "支持r_literacy的目标改写建议。另发现任务1目标与任务2活动存在错位：目标提到'控制变量方法'但任务2中无对应的变量控制环节，仅有定性观察。",
      "refers_to": null
    },
    {
      "round": 1,
      "role_id": "r_design",
      "content": "成果交流节完全缺失，PBL课型的核心结构件缺失，A维度封顶2.0。须补充学生展示环节（AB演讲）和完整的双轨评价量规。",
      "refers_to": null
    },
    {
      "round": 2,
      "role_id": "r_literacy",
      "content": "支持r_content的化学方程式错误判断。补充：该错误会直接导致F1素养目标中'基于证据链构建燃烧调控模型'无法实现，因为错误的方程式会误导学生建立错误模型。",
      "refers_to": "r1:r_content"
    },
    {
      "round": 2,
      "role_id": "r_content",
      "content": "支持r_design关于成果交流节缺失的判断。补充：缺少成果交流节还意味着F6双轨量规无法落地，与我的C维度发现形成交叉影响。",
      "refers_to": "r1:r_design"
    },
    {
      "round": 3,
      "role_id": "r_chair",
      "content": "裁定：(1)目标改写采纳r_literacy方案（F维度优先级最高）；(2)化学方程式与质量比错误均采纳修复（C维度重大错误，内容专家优先）；(3)目标-活动错位问题部分采纳，任务2增加变量控制环节；(4)成果交流节补全，采纳r_design方案并补充双轨量规。",
      "refers_to": ["r1:r_literacy", "r1:r_content", "r1:r_learner", "r1:r_design"]
    }
  ],
  "modifications": [
    {
      "mod_id": "M01",
      "location": "项目目标·第1条",
      "before_summary": "了解蜡烛燃烧原理和化学反应条件",
      "after_summary": "能从露营停电情境中提出燃烧时间问题，拆解影响变量（烛芯/蜡组分/环境），形成'设计调控'问题解决思路",
      "source_role": "r_literacy",
      "rationale": "目标行为化改写（round 1 批注，round 3 采纳），从知识点陈述改为行为化素养表述"
    },
    {
      "mod_id": "M02",
      "location": "任务一·第3节·化学方程式",
      "before_summary": "2C₂₅H₅₂ + 51O₂ → 50CO + 52H₂O（石蜡完全燃烧）",
      "after_summary": "C₂₅H₅₂ + 38O₂ → 25CO₂ + 26H₂O（修正为完全燃烧方程式）",
      "source_role": "r_content",
      "rationale": "重大知识性错误：完全燃烧应生成CO₂而非CO（round 1 r_content批注，round 3 采纳，G2义务修复而非删除）"
    },
    {
      "mod_id": "M03",
      "location": "任务一·质量比计算",
      "before_summary": "石蜡与氧气质量比 816:352",
      "after_summary": "石蜡与氧气质量比 1216:352（C₂₅H₅₂:38O₂=352:1216，简化比=11:38）",
      "source_role": "r_content",
      "rationale": "计算错误订正，连锁后果与M02合并根因处理"
    },
    {
      "mod_id": "M04",
      "location": "（新增）成果交流·班级蜡烛新品发布会",
      "before_summary": "（新增）",
      "after_summary": "新增成果交流节：AB演讲（A组展示蜡烛设计方案+燃烧性能数据，B组提问挑战）；双轨量规（个人成长量规：变量控制能力4级；问题解决量规：燃烧时间达标率3级）",
      "source_role": "r_design",
      "rationale": "PBL课型缺失核心结构件，A维度封顶修复（round 1 r_design批注，round 3 采纳）；同时补全F6双轨量规"
    }
  ]
}
```

---

*本文档版本 v1.0 · 2026-07-16 · 使用 Claude Code 辅助生成，人工校对*

    def _build_annotation_prompt(self, lesson_text, profile):
        return ""  # 主持人不做独立批注
    
    def _build_review_prompt(self, lesson_text, own, others):
        return ""  # 主持人不做互评

```

```
